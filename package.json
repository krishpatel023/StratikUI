{
    "name": "stratikui",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "packageManager": "npm@9.8.0",
    "scripts": {
        "build": "turbo build",
        "dev": "turbo dev",
        "lint": "turbo lint",
        "format": "prettier --write \"**/*.{ts,tsx,md}\"",
        "check-types": "tsc --noEmit"
    },
    "workspaces": [
        "apps/*"
    ],
    "engines": {
        "node": ">=18"
    },
    "devDependencies": {
        "turbo": "^2.0.7"
    }
}
