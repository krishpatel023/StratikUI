import { Implementation, Preview, Header, Display, Wrapper, ImportsWrapper, TechnologyUsed } from "@/components/mdx/MDXServerImports"
import { CodeBlock } from "@/components/mdx/MDXClientImports"

export const Data = {
  name: "Stacked Avatar",
  description: "The Avatar component is a versatile UI element that displays either an image or initials within a circular container. It can be used to represent a user or entity in various contexts such as user profiles, comment sections, or member lists.",
  version_included: "0.2.1",
  tags: [],
  display: true,
};

<Wrapper>

<ImportsWrapper> 

import { StackedAvatarImplementation } from "@/packages/primitives/avatar/Avatar_3";

</ImportsWrapper>

<Header />

<Preview> 
<Display>
  <StackedAvatarImplementation /> 
</Display>
</Preview>

<Implementation>
<TechnologyUsed technologies={[ "tailwind-css" , "twMerge" ]}/>

<CodeBlock> 
```tsx|Avatar
"use client";

import Image, { StaticImageData } from "next/image";
import Dummy from "@/assets/Images/Image_2.jpg";
import { twMerge } from "tailwind-merge";

interface AvatarProps {
  src?: string | StaticImageData;
  alt?: string;
  className?: string;
  name: string;
  isDisabled?: boolean;
}

export const Avatar = ({
  src,
  alt,
  name,
  className,
  isDisabled,
}: AvatarProps) => {
  const initials = name
    .split(" ")
    .map((word) => word.charAt(0))
    .join("")
    .substring(0, 2);

  return (
    <div
      className={twMerge(
        "size-12 rounded-full flex justify-center items-center group aria-disabled: bg-gradient-to-r from-blue-500 to-purple-500",
        className
      )}
      aria-disabled={isDisabled}
      aria-label="avatar"
    >
      {src ? (
        <Image
          src={src}
          alt={alt || "avatar"}
          className="rounded-[inherit] size-full"
        />
      ) : (
        <h1 aria-label="initials" className="text-2xl text-primary-foreground">
          {initials}
        </h1>
      )}
    </div>
  );
};

export const AvatarStack = ({
  avatars,
  limit = 5,
  withExtension,
}: {
  avatars: AvatarProps[];
  limit?: number;
  withExtension?: boolean;
}) => {
  const displayedAvatars = avatars.slice(0, limit);
  const remainingCount = avatars.length - limit;

  return (
    <div className="flex flex-row-reverse">
      {withExtension && remainingCount > 0 && (
        <div className="z-10 -ml-4 last:ml-0">
          <Avatar name={`+ ${remainingCount}`} />
        </div>
      )}
      {displayedAvatars.map((avatar, index) => (
        <div
          key={index}
          className="-ml-4 last:ml-0"
          style={{ zIndex: displayedAvatars.length - index }}
        >
          <Avatar {...avatar} />
        </div>
      ))}
    </div>
  );
};
```
```jsx|Avatar
"use client";

import Image, { StaticImageData } from "next/image";
import Dummy from "@/assets/Images/Image_2.jpg";
import { twMerge } from "tailwind-merge";

export const Avatar = ({
  src,
  alt,
  name,
  className,
  isDisabled,
}) => {
  const initials = name
    .split(" ")
    .map((word) => word.charAt(0))
    .join("")
    .substring(0, 2);

  return (
    <div
      className={twMerge(
        "size-12 rounded-full flex justify-center items-center group aria-disabled: bg-gradient-to-r from-blue-500 to-purple-500",
        className
      )}
      aria-disabled={isDisabled}
      aria-label="avatar"
    >
      {src ? (
        <Image
          src={src}
          alt={alt || "avatar"}
          className="rounded-[inherit] size-full"
        />
      ) : (
        <h1 aria-label="initials" className="text-2xl text-primary-foreground">
          {initials}
        </h1>
      )}
    </div>
  );
};

export const AvatarStack = ({
  avatars,
  limit = 5,
  withExtension,
}) => {
  const displayedAvatars = avatars.slice(0, limit);
  const remainingCount = avatars.length - limit;

  return (
    <div className="flex flex-row-reverse">
      {withExtension && remainingCount > 0 && (
        <div className="z-10 -ml-4 last:ml-0">
          <Avatar name={`+ ${remainingCount}`} />
        </div>
      )}
      {displayedAvatars.map((avatar, index) => (
        <div
          key={index}
          className="-ml-4 last:ml-0"
          style={{ zIndex: displayedAvatars.length - index }}
        >
          <Avatar {...avatar} />
        </div>
      ))}
    </div>
  );
};
```
```tsx|Implementation
export function StackedAvatarImplementation() {
  return (
    <div className="flex flex-col justify-center items-center gap-4">
      {/* With Extension */}
      <ArrowHeading text="With Extension" />
      <AvatarStack
        avatars={[
          { src: Dummy6, name: "Profile Picture" },
          { src: Dummy2, name: "Profile Picture" },
          { src: Dummy3, name: "Profile Picture" },
          { src: Dummy4, name: "Profile Picture" },
          { src: Dummy5, name: "Profile Picture" },
          { src: Dummy1, name: "Profile Picture" },
          { src: Dummy7, name: "Profile Picture" },
        ]}
        withExtension
      />
      {/* Without Extension */}
      <ArrowHeading text="Without Extension" />
      <AvatarStack
        avatars={[
          { src: Dummy2, name: "Profile Picture" },
          { src: Dummy3, name: "Profile Picture" },
          { src: Dummy4, name: "Profile Picture" },
          { src: Dummy7, name: "Profile Picture" },
          { src: Dummy5, name: "Profile Picture" },
          { src: Dummy6, name: "Profile Picture" },
          { src: Dummy1, name: "Profile Picture" },
        ]}
      />
    </div>
  );
}
```
```jsx|Implementation
export function StackedAvatarImplementation() {
  return (
    <div className="flex flex-col justify-center items-center gap-4">
      {/* With Extension */}
      <ArrowHeading text="With Extension" />
      <AvatarStack
        avatars={[
          { src: Dummy6, name: "Profile Picture" },
          { src: Dummy2, name: "Profile Picture" },
          { src: Dummy3, name: "Profile Picture" },
          { src: Dummy4, name: "Profile Picture" },
          { src: Dummy5, name: "Profile Picture" },
          { src: Dummy1, name: "Profile Picture" },
          { src: Dummy7, name: "Profile Picture" },
        ]}
        withExtension
      />
      {/* Without Extension */}
      <ArrowHeading text="Without Extension" />
      <AvatarStack
        avatars={[
          { src: Dummy2, name: "Profile Picture" },
          { src: Dummy3, name: "Profile Picture" },
          { src: Dummy4, name: "Profile Picture" },
          { src: Dummy7, name: "Profile Picture" },
          { src: Dummy5, name: "Profile Picture" },
          { src: Dummy6, name: "Profile Picture" },
          { src: Dummy1, name: "Profile Picture" },
        ]}
      />
    </div>
  );
}
```
</CodeBlock>

## Props

| Name       | Type                        | Description                                                                                        |
|------------|-----------------------------|----------------------------------------------------------------------------------------------------|
| src        | string \| StaticImageData   | The source URL or imported static image data for the avatar image. If not provided, initials will be displayed. |
| alt        | string                      | Alternative text for the avatar image. Defaults to "avatar" if not provided.                       |
| className  | string                      | Additional CSS classes to be applied to the avatar container.                                      |
| name       | string                      | The name used to generate initials when no image is provided.                                      |
| isDisabled | boolean                     | When true, applies a disabled state to the avatar.                                                 |


## Stacked Avatar Props

| Prop | Type | Description |
|------|------|-------------|
| avatars | AvatarProps[] | An array of avatar objects. Each object should contain props for the Avatar component. |
| limit | number | The maximum number of avatars to display in the stack. |
| withExtension | boolean | If true, shows an additional avatar with the count of remaining avatars when there are more avatars than the limit. |

</Implementation>
</Wrapper>