import React, { Suspense, lazy } from 'react';
import { Implementation, Preview, WrapperToggle, TechnologyUsed, Wrapper, Skeleton, PARTITION, Details, Provider, Title } from "@/components/mdx/MDXServerImports"
import { CodeBlock, ResizableDisplay } from "@/components/mdx/MDXClientImports"

{/*
// ------------------------------------------------------------------------------------
    //	The text below is generated from "@/registry/components/pricing/01"
//------------------------------------------------------------------------------------
*/}

<Wrapper>
<Title>Pricing Group with Spotlight Animations</Title>
### The pricing group component features an engaging background with a dynamic circular gradient that seamlessly follows the mouse cursor, creating an interactive and visually appealing experience. As users move their cursor across the component, the gradient shifts and transforms, accompanied by a sleek border animation that enhances the sense of motion and responsiveness.
<WrapperToggle>
<Preview>

export const Components_pricing_01 = lazy(() => import("@registry/packages/components/pricing/01/default-ts/example"));


<Suspense fallback={<Skeleton className="h-[800px] rounded-lg" />}>
<ResizableDisplay>
<Components_pricing_01 />
</ResizableDisplay>
</Suspense>

</Preview><Implementation>
undefined
<CodeBlock>

```default|js|example.jsx
import { Pricing } from "@registry/packages/components/pricing/01/default-js/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full min-h-[35rem] flex justify-center items-center py-10">
      <Pricing />
    </div>
  );
}

```

```default|js|Pricing.jsx
import { Button } from "@registry/packages/primitives/buttons/02/default-js/Button";
import { HighlighterItem, HighlightGroup } from "@registry/packages/primitives/containers/03/default-js/Container";
import { twMerge } from "tailwind-merge";

export function Pricing() {
  const PricingData = [
    {
      name: "Basic",
      description: "For small teams and individuals",
      price: 10,
      features: ["10 users included", "2 GB of storage"],
      popular: false,
    },
    {
      name: "Pro",
      description: "For startups",
      price: 29,
      features: ["1000 users included", "20 GB of storage", "Community access"],
      popular: true,
    },
    {
      name: "Business",
      description: "For Enterprises",
      price: 49,
      features: ["Unlimited Users", "100 GB of storage", "Help center access", "Email support"],
      popular: false,
    },
  ];

  return (
    <HighlightGroup className="flex flex-col @lg:flex-row justify-center items-center gap-10">
      {PricingData.map((data, index) => (
        <div key={index} className="w-max relative">
          {data.popular && (
            <div className="absolute -top-4 text-primary-foreground w-full flex justify-center items-center z-50">
              <h1 className="px-4 py-1 text-sm font-semibold rounded-full bg-foreground text-background">
                Most Popular
              </h1>
            </div>
          )}
          <HighlighterItem className={twMerge(" w-80 h-[27rem]", data.popular && " bg-secondary")}>
            <div className="text-primary-foreground px-8 py-6 w-full h-full flex flex-col justify-between">
              <div>
                <h1 className="text-3xl font-medium mb-2">{data.name}</h1>
                <h2 className="text-md mb-4">{data.description}</h2>
                <div className="w-full h-[1px] bg-outline " />
                <h1 className="text-4xl font-medium mb-8 mt-6">
                  $ {data.price} <span className="text-secondary-foreground text-base">/ Month</span>
                </h1>
                <span className="flex flex-col gap-2">
                  {data.features.map((feature, j) => (
                    <span className="flex justify-start items-center gap-2" key={j}>
                      <Check /> {feature}
                    </span>
                  ))}
                </span>
              </div>
              <Button variant="primary">Get Started</Button>
            </div>
          </HighlighterItem>
        </div>
      ))}
    </HighlightGroup>
  );
}

export const Check = (props) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

```

```default|ts|example.tsx
import { Pricing } from "@registry/packages/components/pricing/01/default-ts/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full min-h-[35rem] flex justify-center items-center py-10">
      <Pricing />
    </div>
  );
}

```

```default|ts|Pricing.tsx
import { Button } from "@registry/packages/primitives/buttons/02/default-ts/Button";
import { HighlighterItem, HighlightGroup } from "@registry/packages/primitives/containers/03/default-ts/Container";
import type { IconProps } from "@registry/utils/types";
import { twMerge } from "tailwind-merge";

export function Pricing() {
  const PricingData = [
    {
      name: "Basic",
      description: "For small teams and individuals",
      price: 10,
      features: ["10 users included", "2 GB of storage"],
      popular: false,
    },
    {
      name: "Pro",
      description: "For startups",
      price: 29,
      features: ["1000 users included", "20 GB of storage", "Community access"],
      popular: true,
    },
    {
      name: "Business",
      description: "For Enterprises",
      price: 49,
      features: ["Unlimited Users", "100 GB of storage", "Help center access", "Email support"],
      popular: false,
    },
  ];

  return (
    <HighlightGroup className="flex flex-col @lg:flex-row justify-center items-center gap-10">
      {PricingData.map((data, index) => (
        <div key={index} className="w-max relative">
          {data.popular && (
            <div className="absolute -top-4 text-primary-foreground w-full flex justify-center items-center z-50">
              <h1 className="px-4 py-1 text-sm font-semibold rounded-full bg-foreground text-background">
                Most Popular
              </h1>
            </div>
          )}
          <HighlighterItem className={twMerge(" w-80 h-[27rem]", data.popular && " bg-secondary")}>
            <div className="text-primary-foreground px-8 py-6 w-full h-full flex flex-col justify-between">
              <div>
                <h1 className="text-3xl font-medium mb-2">{data.name}</h1>
                <h2 className="text-md mb-4">{data.description}</h2>
                <div className="w-full h-[1px] bg-outline " />
                <h1 className="text-4xl font-medium mb-8 mt-6">
                  $ {data.price} <span className="text-secondary-foreground text-base">/ Month</span>
                </h1>
                <span className="flex flex-col gap-2">
                  {data.features.map((feature, j) => (
                    <span className="flex justify-start items-center gap-2" key={j}>
                      <Check /> {feature}
                    </span>
                  ))}
                </span>
              </div>
              <Button variant="primary">Get Started</Button>
            </div>
          </HighlighterItem>
        </div>
      ))}
    </HighlightGroup>
  );
}

export const Check = (props: IconProps) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

```

</CodeBlock>
<Details>


</Details>
</Implementation>
</WrapperToggle>
</Wrapper>

{/*
// ------------------------------------------------------------------------------------
    //	The text below is generated from "@/registry/components/pricing/02"
//------------------------------------------------------------------------------------
*/}

<Wrapper>
<Title>Simple Pricing Component</Title>
### The simple pricing component features a clean and straightforward design, making it easy for users to find the information they need without any distractions.
<WrapperToggle>
<Preview>

export const Components_pricing_02 = lazy(() => import("@registry/packages/components/pricing/02/default-ts/example"));


<Suspense fallback={<Skeleton className="h-[800px] rounded-lg" />}>
<ResizableDisplay>
<Components_pricing_02 />
</ResizableDisplay>
</Suspense>

</Preview><Implementation>
undefined
<CodeBlock>

```default|js|example.jsx
import { Pricing } from "@registry/packages/components/pricing/02/default-js/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full min-h-[35rem] flex justify-center items-center py-10">
      <Pricing />
    </div>
  );
}

```

```default|js|Pricing.jsx
import { Button } from "@registry/packages/primitives/buttons/02/default-js/Button";
import { twMerge } from "tailwind-merge";

export function Pricing() {
  const PricingData = [
    {
      name: "Basic",
      description: "For small teams and individuals",
      price: 10,
      features: ["10 users included", "2 GB of storage"],
      popular: false,
    },
    {
      name: "Pro",
      description: "For startups",
      price: 29,
      features: ["1000 users included", "20 GB of storage", "Community access"],
      popular: true,
    },
    {
      name: "Business",
      description: "For Enterprises",
      price: 49,
      features: ["Unlimited Users", "100 GB of storage", "Help center access", "Email support"],
      popular: false,
    },
  ];

  return (
    <div className="flex flex-col @lg:flex-row justify-center items-center gap-10">
      {PricingData.map((data, index) => (
        <div key={index} className="w-max relative">
          {data.popular && (
            <div className="absolute -top-4 text-primary-foreground w-full flex justify-center items-center z-50">
              <h1 className="px-4 py-1 text-sm font-semibold rounded-full bg-foreground text-background">
                Most Popular
              </h1>
            </div>
          )}
          <div
            className={twMerge(
              "rounded-lg w-80 h-[27rem] bg-primary border border-outline",
              data.popular && "border-2 border-foreground",
            )}
          >
            <div className="text-primary-foreground px-8 py-6 w-full h-full flex flex-col justify-between">
              <div>
                <h1 className="text-3xl font-medium mb-2">{data.name}</h1>
                <h2 className="text-md mb-4">{data.description}</h2>
                <div className="w-full h-[1px] bg-outline " />
                <h1 className="text-4xl font-medium mb-8 mt-6">
                  $ {data.price} <span className="text-secondary-foreground text-base">/ Month</span>
                </h1>
                <span className="flex flex-col gap-2">
                  {data.features.map((feature, j) => (
                    <span className="flex justify-start items-center gap-2" key={j}>
                      <Check /> {feature}
                    </span>
                  ))}
                </span>
              </div>
              <Button variant="primary">Get Started</Button>
            </div>
          </div>
        </div>
      ))}
    </div>
  );
}

export const Check = (props) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

```

```default|ts|example.tsx
import { Pricing } from "@registry/packages/components/pricing/02/default-ts/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full min-h-[35rem] flex justify-center items-center py-10">
      <Pricing />
    </div>
  );
}

```

```default|ts|Pricing.tsx
import { Button } from "@registry/packages/primitives/buttons/02/default-ts/Button";
import type { IconProps } from "@registry/utils/types";
import { twMerge } from "tailwind-merge";

export function Pricing() {
  const PricingData = [
    {
      name: "Basic",
      description: "For small teams and individuals",
      price: 10,
      features: ["10 users included", "2 GB of storage"],
      popular: false,
    },
    {
      name: "Pro",
      description: "For startups",
      price: 29,
      features: ["1000 users included", "20 GB of storage", "Community access"],
      popular: true,
    },
    {
      name: "Business",
      description: "For Enterprises",
      price: 49,
      features: ["Unlimited Users", "100 GB of storage", "Help center access", "Email support"],
      popular: false,
    },
  ];

  return (
    <div className="flex flex-col @lg:flex-row justify-center items-center gap-10">
      {PricingData.map((data, index) => (
        <div key={index} className="w-max relative">
          {data.popular && (
            <div className="absolute -top-4 text-primary-foreground w-full flex justify-center items-center z-50">
              <h1 className="px-4 py-1 text-sm font-semibold rounded-full bg-foreground text-background">
                Most Popular
              </h1>
            </div>
          )}
          <div
            className={twMerge(
              "rounded-lg w-80 h-[27rem] bg-primary border border-outline",
              data.popular && "border-2 border-foreground",
            )}
          >
            <div className="text-primary-foreground px-8 py-6 w-full h-full flex flex-col justify-between">
              <div>
                <h1 className="text-3xl font-medium mb-2">{data.name}</h1>
                <h2 className="text-md mb-4">{data.description}</h2>
                <div className="w-full h-[1px] bg-outline " />
                <h1 className="text-4xl font-medium mb-8 mt-6">
                  $ {data.price} <span className="text-secondary-foreground text-base">/ Month</span>
                </h1>
                <span className="flex flex-col gap-2">
                  {data.features.map((feature, j) => (
                    <span className="flex justify-start items-center gap-2" key={j}>
                      <Check /> {feature}
                    </span>
                  ))}
                </span>
              </div>
              <Button variant="primary">Get Started</Button>
            </div>
          </div>
        </div>
      ))}
    </div>
  );
}

export const Check = (props: IconProps) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

```

</CodeBlock>
<Details>


</Details>
</Implementation>
</WrapperToggle>
</Wrapper>

{/*
// ------------------------------------------------------------------------------------
    //	The text below is generated from "@/registry/components/pricing/03"
//------------------------------------------------------------------------------------
*/}

<Wrapper>
<Title>Pricing with Accent Differentiator for Most Popular Plan</Title>
### The simple pricing component features a clean and straightforward design, making it easy for users to find the information they need without any distractions.
<WrapperToggle>
<Preview>

export const Components_pricing_03 = lazy(() => import("@registry/packages/components/pricing/03/default-ts/example"));


<Suspense fallback={<Skeleton className="h-[800px] rounded-lg" />}>
<ResizableDisplay>
<Components_pricing_03 />
</ResizableDisplay>
</Suspense>

</Preview><Implementation>
undefined
<CodeBlock>

```default|js|example.jsx
import { Pricing } from "@registry/packages/components/pricing/03/default-js/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full min-h-[35rem] flex justify-center items-center py-10">
      <Pricing />
    </div>
  );
}

```

```default|js|Pricing.jsx
import { Button } from "@registry/packages/primitives/buttons/02/default-js/Button";
import { twMerge } from "tailwind-merge";

export function Pricing() {
  const PricingData = [
    {
      name: "Basic",
      description: "For small teams and individuals",
      price: 10,
      features: ["10 users included", "2 GB of storage"],
      popular: false,
    },
    {
      name: "Pro",
      description: "For startups",
      price: 29,
      features: ["1000 users included", "20 GB of storage", "Community access"],
      popular: true,
    },
    {
      name: "Business",
      description: "For Enterprises",
      price: 49,
      features: ["Unlimited Users", "100 GB of storage", "Help center access", "Email support"],
      popular: false,
    },
  ];

  return (
    <div className="flex flex-col @lg:flex-row justify-center items-center gap-10">
      {PricingData.map((data, index) => (
        <div
          className={twMerge("rounded-lg w-80 h-[27rem] bg-primary border border-outline-secondary shadow")}
          key={index}
        >
          <div className="text-primary-foreground px-8 py-6 w-full h-full flex flex-col justify-between">
            <div>
              <span className="text-3xl font-medium mb-2 flex justify-between">
                {data.name}
                {data.popular && (
                  <h1 className="px-4 text-sm font-semibold rounded-full bg-blue-200 dark:bg-blue-600/40 text-blue-800 dark:text-blue-400 flex  items-center">
                    Most Popular
                  </h1>
                )}
              </span>
              <h2 className="text-md mb-4">{data.description}</h2>
              <div className="w-full h-[1px] bg-outline" />
              <h1 className="text-4xl font-medium mb-8 mt-6">
                $ {data.price} <span className="text-secondary-foreground text-base">/ Month</span>
              </h1>
              <span className="flex flex-col gap-2">
                {data.features.map((feature, j) => (
                  <span className="flex justify-start items-center gap-2" key={j}>
                    <Check /> {feature}
                  </span>
                ))}
              </span>
            </div>
            <Button variant={data.popular ? "accent" : "primary"}>Get Started</Button>
          </div>
        </div>
      ))}
    </div>
  );
}

export const Check = (props) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

```

```default|ts|example.tsx
import { Pricing } from "@registry/packages/components/pricing/03/default-ts/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full min-h-[35rem] flex justify-center items-center py-10">
      <Pricing />
    </div>
  );
}

```

```default|ts|Pricing.tsx
import { Button } from "@registry/packages/primitives/buttons/02/default-ts/Button";
import type { IconProps } from "@registry/utils/types";
import { twMerge } from "tailwind-merge";

export function Pricing() {
  const PricingData = [
    {
      name: "Basic",
      description: "For small teams and individuals",
      price: 10,
      features: ["10 users included", "2 GB of storage"],
      popular: false,
    },
    {
      name: "Pro",
      description: "For startups",
      price: 29,
      features: ["1000 users included", "20 GB of storage", "Community access"],
      popular: true,
    },
    {
      name: "Business",
      description: "For Enterprises",
      price: 49,
      features: ["Unlimited Users", "100 GB of storage", "Help center access", "Email support"],
      popular: false,
    },
  ];

  return (
    <div className="flex flex-col @lg:flex-row justify-center items-center gap-10">
      {PricingData.map((data, index) => (
        <div
          className={twMerge("rounded-lg w-80 h-[27rem] bg-primary border border-outline-secondary shadow")}
          key={index}
        >
          <div className="text-primary-foreground px-8 py-6 w-full h-full flex flex-col justify-between">
            <div>
              <span className="text-3xl font-medium mb-2 flex justify-between">
                {data.name}
                {data.popular && (
                  <h1 className="px-4 text-sm font-semibold rounded-full bg-blue-200 dark:bg-blue-600/40 text-blue-800 dark:text-blue-400 flex  items-center">
                    Most Popular
                  </h1>
                )}
              </span>
              <h2 className="text-md mb-4">{data.description}</h2>
              <div className="w-full h-[1px] bg-outline" />
              <h1 className="text-4xl font-medium mb-8 mt-6">
                $ {data.price} <span className="text-secondary-foreground text-base">/ Month</span>
              </h1>
              <span className="flex flex-col gap-2">
                {data.features.map((feature, j) => (
                  <span className="flex justify-start items-center gap-2" key={j}>
                    <Check /> {feature}
                  </span>
                ))}
              </span>
            </div>
            <Button variant={data.popular ? "accent" : "primary"}>Get Started</Button>
          </div>
        </div>
      ))}
    </div>
  );
}

export const Check = (props: IconProps) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

```

</CodeBlock>
<Details>


</Details>
</Implementation>
</WrapperToggle>
</Wrapper>

{/*
// ------------------------------------------------------------------------------------
    //	The text below is generated from "@/registry/components/pricing/04"
//------------------------------------------------------------------------------------
*/}

<Wrapper>
<Title>Pricing with Highlighted Plan</Title>
### The simple pricing component features a clean and straightforward design, making it easy for users to find the information they need without any distractions. It has a highlighted plan that is easy to spot and distinguish from the other plans.
<WrapperToggle>
<Preview>

export const Components_pricing_04 = lazy(() => import("@registry/packages/components/pricing/04/default-ts/example"));


<Suspense fallback={<Skeleton className="h-[800px] rounded-lg" />}>
<ResizableDisplay>
<Components_pricing_04 />
</ResizableDisplay>
</Suspense>

</Preview><Implementation>
undefined
<CodeBlock>

```default|js|example.jsx
import { Pricing } from "@registry/packages/components/pricing/04/default-js/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full min-h-[35rem] flex justify-center items-center py-10">
      <Pricing />
    </div>
  );
}

```

```default|js|Pricing.jsx
import { Button } from "@registry/packages/primitives/buttons/02/default-js/Button";
import { twMerge } from "tailwind-merge";

export function Pricing() {
  const PricingData = [
    {
      name: "Basic",
      description: "For small teams and individuals",
      price: 10,
      features: ["10 users included", "2 GB of storage"],
      popular: false,
    },
    {
      name: "Pro",
      description: "For startups",
      price: 29,
      features: ["1000 users included", "20 GB of storage", "Community access"],
      popular: true,
    },
    {
      name: "Business",
      description: "For Enterprises",
      price: 49,
      features: ["Unlimited Users", "100 GB of storage", "Help center access", "Email support"],
      popular: false,
    },
  ];

  return (
    <div className="flex flex-col @lg:flex-row justify-center items-end gap-10">
      {PricingData.map((data, index) => (
        <div
          className={twMerge(
            "rounded-lg w-80 h-[27rem] text-primary-foreground bg-primary border border-outline-secondary shadow",
            data.popular && "bg-accent text-accent-foreground h-[30rem] border-none shadow-accent-secondary",
          )}
          key={index}
        >
          <div className="px-8 py-6 w-full h-full flex flex-col justify-between">
            <div>
              <h1 className="text-3xl font-medium mb-2">{data.name}</h1>
              <h2 className="text-md mb-4">{data.description}</h2>
              <div className={twMerge("w-full h-[1px] bg-outline", data.popular && "bg-accent-foreground")} />
              <h1 className="text-4xl font-medium mb-8 mt-6">
                $ {data.price} <span className="text-base">/ Month</span>
              </h1>
              <span className="flex flex-col gap-2">
                {data.features.map((feature, j) => (
                  <span className="flex justify-start items-center gap-2" key={j}>
                    <Check /> {feature}
                  </span>
                ))}
              </span>
            </div>
            <Button variant={data.popular ? "complementary" : "primary"}>Get Started</Button>
          </div>
        </div>
      ))}
    </div>
  );
}

export const Check = (props) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

```

```default|ts|example.tsx
import { Pricing } from "@registry/packages/components/pricing/04/default-ts/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full min-h-[35rem] flex justify-center items-center py-10">
      <Pricing />
    </div>
  );
}

```

```default|ts|Pricing.tsx
import { Button } from "@registry/packages/primitives/buttons/02/default-ts/Button";
import type { IconProps } from "@registry/utils/types";
import { twMerge } from "tailwind-merge";

export function Pricing() {
  const PricingData = [
    {
      name: "Basic",
      description: "For small teams and individuals",
      price: 10,
      features: ["10 users included", "2 GB of storage"],
      popular: false,
    },
    {
      name: "Pro",
      description: "For startups",
      price: 29,
      features: ["1000 users included", "20 GB of storage", "Community access"],
      popular: true,
    },
    {
      name: "Business",
      description: "For Enterprises",
      price: 49,
      features: ["Unlimited Users", "100 GB of storage", "Help center access", "Email support"],
      popular: false,
    },
  ];

  return (
    <div className="flex flex-col @lg:flex-row justify-center items-end gap-10">
      {PricingData.map((data, index) => (
        <div
          className={twMerge(
            "rounded-lg w-80 h-[27rem] text-primary-foreground bg-primary border border-outline-secondary shadow",
            data.popular && "bg-accent text-accent-foreground h-[30rem] border-none shadow-accent-secondary",
          )}
          key={index}
        >
          <div className="px-8 py-6 w-full h-full flex flex-col justify-between">
            <div>
              <h1 className="text-3xl font-medium mb-2">{data.name}</h1>
              <h2 className="text-md mb-4">{data.description}</h2>
              <div className={twMerge("w-full h-[1px] bg-outline", data.popular && "bg-accent-foreground")} />
              <h1 className="text-4xl font-medium mb-8 mt-6">
                $ {data.price} <span className="text-base">/ Month</span>
              </h1>
              <span className="flex flex-col gap-2">
                {data.features.map((feature, j) => (
                  <span className="flex justify-start items-center gap-2" key={j}>
                    <Check /> {feature}
                  </span>
                ))}
              </span>
            </div>
            <Button variant={data.popular ? "complementary" : "primary"}>Get Started</Button>
          </div>
        </div>
      ))}
    </div>
  );
}

export const Check = (props: IconProps) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

```

</CodeBlock>
<Details>


</Details>
</Implementation>
</WrapperToggle>
</Wrapper>

{/*
// ------------------------------------------------------------------------------------
    //	The text below is generated from "@/registry/components/pricing/05"
//------------------------------------------------------------------------------------
*/}

<Wrapper>
<Title>Pricing with more rounded design</Title>
### The simple pricing component features a clean and straightforward design, making it easy for users to find the information they need without any distractions. It has a highlighted plan that is easy to spot.
<WrapperToggle>
<Preview>

export const Components_pricing_05 = lazy(() => import("@registry/packages/components/pricing/05/default-ts/example"));


<Suspense fallback={<Skeleton className="h-[800px] rounded-lg" />}>
<ResizableDisplay>
<Components_pricing_05 />
</ResizableDisplay>
</Suspense>

</Preview><Implementation>
undefined
<CodeBlock>

```default|js|example.jsx
import { Pricing } from "@registry/packages/components/pricing/05/default-js/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full min-h-[35rem] flex justify-center items-center py-10">
      <Pricing />
    </div>
  );
}

```

```default|js|Pricing.jsx
import { Button } from "@registry/packages/primitives/buttons/02/default-js/Button";
import { twMerge } from "tailwind-merge";

export function Pricing() {
  const PricingData = [
    {
      name: "Basic",
      description: "For individuals",
      price: 0,
      features: ["100 users included", "10 GB of storage", "10,000 Requests / month"],
      popular: false,
    },
    {
      name: "Pro",
      description: "For startups",
      price: 29,
      features: ["10000 users included", "100 GB of storage", "10,00,000 Requests / month"],
      popular: true,
    },
    {
      name: "Enterprise",
      description: "For Enterprises",
      price: 49,
      features: ["Unlimited Users", "500 GB of storage", "Unlimited Requests / month", "Email support"],
      popular: false,
    },
  ];

  return (
    <div className="flex flex-col @lg:flex-row justify-center items-center gap-6">
      {PricingData.map((data, index) => (
        <div
          className={twMerge("rounded-xl w-80 h-[27rem] bg-primary border border-outline-secondary shadow")}
          key={index}
        >
          <div className="text-primary-foreground px-8 py-6 w-full h-full flex flex-col justify-between">
            <div>
              <span className="text-2xl font-medium mb-2 flex justify-between">
                {data.name}
                {data.popular && (
                  <h1 className="h-8 px-4 text-sm font-semibold rounded-full bg-blue-200 dark:bg-blue-600/40 text-blue-800 dark:text-blue-400 flex  items-center">
                    Most Popular
                  </h1>
                )}
              </span>
              <h2 className="text-md mb-4 text-secondary-foreground font-medium">{data.description}</h2>
              <div className="w-full h-[1px] bg-outline " />
              <h1 className="text-4xl font-medium mb-8 mt-6 flex gap-2 items-end">
                ${data.price}
                <span className="text-secondary-foreground font-medium text-base">/ Month</span>
              </h1>
              <span className="flex flex-col gap-2">
                {data.features.map((feature, j) => (
                  <span className="flex justify-start items-center gap-2 text-secondary-foreground font-medium" key={j}>
                    <Check className="text-accent" /> {feature}
                  </span>
                ))}
              </span>
            </div>
            <Button variant={data.popular ? "accent" : "primary"} className="rounded-lg">
              Get Started
            </Button>
          </div>
        </div>
      ))}
    </div>
  );
}

export const Check = (props) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

```

```default|ts|example.tsx
import { Pricing } from "@registry/packages/components/pricing/05/default-ts/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full min-h-[35rem] flex justify-center items-center py-10">
      <Pricing />
    </div>
  );
}

```

```default|ts|Pricing.tsx
import { Button } from "@registry/packages/primitives/buttons/02/default-ts/Button";
import type { IconProps } from "@registry/utils/types";
import { twMerge } from "tailwind-merge";

export function Pricing() {
  const PricingData = [
    {
      name: "Basic",
      description: "For individuals",
      price: 0,
      features: ["100 users included", "10 GB of storage", "10,000 Requests / month"],
      popular: false,
    },
    {
      name: "Pro",
      description: "For startups",
      price: 29,
      features: ["10000 users included", "100 GB of storage", "10,00,000 Requests / month"],
      popular: true,
    },
    {
      name: "Enterprise",
      description: "For Enterprises",
      price: 49,
      features: ["Unlimited Users", "500 GB of storage", "Unlimited Requests / month", "Email support"],
      popular: false,
    },
  ];

  return (
    <div className="flex flex-col @lg:flex-row justify-center items-center gap-6">
      {PricingData.map((data, index) => (
        <div
          className={twMerge("rounded-xl w-80 h-[27rem] bg-primary border border-outline-secondary shadow")}
          key={index}
        >
          <div className="text-primary-foreground px-8 py-6 w-full h-full flex flex-col justify-between">
            <div>
              <span className="text-2xl font-medium mb-2 flex justify-between">
                {data.name}
                {data.popular && (
                  <h1 className="h-8 px-4 text-sm font-semibold rounded-full bg-blue-200 dark:bg-blue-600/40 text-blue-800 dark:text-blue-400 flex  items-center">
                    Most Popular
                  </h1>
                )}
              </span>
              <h2 className="text-md mb-4 text-secondary-foreground font-medium">{data.description}</h2>
              <div className="w-full h-[1px] bg-outline " />
              <h1 className="text-4xl font-medium mb-8 mt-6 flex gap-2 items-end">
                ${data.price}
                <span className="text-secondary-foreground font-medium text-base">/ Month</span>
              </h1>
              <span className="flex flex-col gap-2">
                {data.features.map((feature, j) => (
                  <span className="flex justify-start items-center gap-2 text-secondary-foreground font-medium" key={j}>
                    <Check className="text-accent" /> {feature}
                  </span>
                ))}
              </span>
            </div>
            <Button variant={data.popular ? "accent" : "primary"} className="rounded-lg">
              Get Started
            </Button>
          </div>
        </div>
      ))}
    </div>
  );
}

export const Check = (props: IconProps) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

```

</CodeBlock>
<Details>


</Details>
</Implementation>
</WrapperToggle>
</Wrapper>

{/*
// ------------------------------------------------------------------------------------
    //	The text below is generated from "@/registry/components/pricing/06"
//------------------------------------------------------------------------------------
*/}

<Wrapper>
<Title>Detailed Pricing Component</Title>
### The Detailed Pricing Component provides an in-depth look at product offerings, featuring multiple sections that clearly outline the features, benefits, and pricing tiers. Each section is designed to present essential information in an organized manner, helping users easily compare different plans.
<WrapperToggle>
<Preview>

export const Components_pricing_06 = lazy(() => import("@registry/packages/components/pricing/06/default-ts/example"));


<Suspense fallback={<Skeleton className="h-[800px] rounded-lg" />}>
<ResizableDisplay>
<Components_pricing_06 />
</ResizableDisplay>
</Suspense>

</Preview><Implementation>
<TechnologyUsed technologies={[]} />
<CodeBlock>

```default|js|example.jsx
import { Pricing } from "@registry/packages/components/pricing/05/default-js/Pricing";
import { PricingGrid } from "@registry/packages/components/pricing/06/default-js/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full flex justify-center items-center">
      <div className="hidden @lg:block w-[90%] mt-40 mb-20">
        <PricingGrid />
      </div>
      <div className="@lg:hidden w-[90%] mt-40 mb-20">
        <Pricing />
      </div>
    </div>
  );
}

```

```default|js|Pricing.jsx
import { Button } from "@registry/packages/primitives/buttons/02/default-js/Button";

export function PricingGrid() {
  const PricingData = [
    {
      name: "Content",
      value: null,
    },
    {
      name: "Number of Users",
      value: ["100", "10,000", "Unlimited"],
    },
    {
      name: "Storage",
      value: ["10 GB", "100 GB", "500 GB"],
    },
    {
      name: "Requests per month",
      value: ["10,000", "10,00,000", "Unlimited"],
    },
    {
      name: "Team members",
      value: ["Just You", "Upto 10", "Unlimited"],
    },
    {
      name: "Early Access",
      value: [false, true, true],
    },
    {
      name: "Support",
      value: null,
    },
    {
      name: "Email Support",
      value: [false, true, true],
    },
    {
      name: "Phone Support",
      value: [false, true, true],
    },
    {
      name: "24/7 Support",
      value: [false, false, true],
    },
  ];

  return (
    <table className="w-full text-foreground">
      <colgroup>
        <col className="w-1/4" />
        <col className="w-1/4" />
        <col className="w-1/4" />
        <col className="w-1/4" />
      </colgroup>
      <thead>
        <td />
        <TableHead name="Basic" targetAudience="For individuals" Amount="0" ButtonText="Get Started" popular={false} />

        <TableHead name="Pro" targetAudience="For Startups" Amount="29" ButtonText="Purchase Now" popular={true} />

        <TableHead
          name="Enterprise"
          targetAudience="For Enterprises"
          Amount="49"
          ButtonText="Contact Sales"
          popular={false}
        />
      </thead>

      <tbody>
        {PricingData.map((data, index) => (
          <TableRow key={index} name={data.name} value={data.value} />
        ))}
      </tbody>
    </table>
  );
}

function TableHead({ name, targetAudience, Amount, ButtonText, popular }) {
  return (
    <td className="px-2 rounded-t-2xl py-6">
      <h1 className="text-2xl font-medium mb-2">{name}</h1>
      <h2 className="text-md mb-4 text-secondary-foreground">{targetAudience}</h2>
      <h1 className="text-4xl font-medium mb-8 mt-6 flex gap-2 items-end">
        ${Amount}
        <span className="text-secondary-foreground font-medium text-base">/ Month</span>
      </h1>
      <Button variant={popular ? "accent" : "primary"} className="w-full">
        {ButtonText}
      </Button>
    </td>
  );
}

function TableRow({ name, value }) {
  return (
    <>
      {value ? (
        <tr className="h-16 text-secondary-foreground">
          <td>{name}</td>
          {value.map((value, index) => (
            // Values of the row
            <td className="text-center" key={index}>
              {typeof value === "string" ? (
                // Incase of string Values
                value
              ) : // In case of boolean values
              value ? (
                <Check className="text-accent mx-auto w-6 h-6" />
              ) : (
                <Cross className="text-secondary-foreground opacity-50 mx-auto w-5 h-5" />
              )}
            </td>
          ))}
        </tr>
      ) : (
        <>
          <tr className="h-20 border-b border-outline-secondary ">
            <td className="font-semibold">
              <div className="w-full min-h-12 " />
              {name}

              <div className="w-full min-h-6 " />
            </td>
            <td />
            <td />
            <td />
          </tr>
        </>
      )}
    </>
  );
}

export const Check = (props) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

export const Cross = (props) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 15 15" {...props}>
    <title>Cross</title>
    <path
      fill="currentColor"
      d="M3.64 2.27L7.5 6.13l3.84-3.84A.92.92 0 0 1 12 2a1 1 0 0 1 1 1a.9.9 0 0 1-.27.66L8.84 7.5l3.89 3.89A.9.9 0 0 1 13 12a1 1 0 0 1-1 1a.92.92 0 0 1-.69-.27L7.5 8.87l-3.85 3.85A.92.92 0 0 1 3 13a1 1 0 0 1-1-1a.9.9 0 0 1 .27-.66L6.16 7.5L2.27 3.61A.9.9 0 0 1 2 3a1 1 0 0 1 1-1c.24.003.47.1.64.27Z"
    />
  </svg>
);

```

```default|ts|example.tsx
import { Pricing } from "@registry/packages/components/pricing/05/default-ts/Pricing";
import { PricingGrid } from "@registry/packages/components/pricing/06/default-ts/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full flex justify-center items-center">
      <div className="hidden @lg:block w-[90%] mt-40 mb-20">
        <PricingGrid />
      </div>
      <div className="@lg:hidden w-[90%] mt-40 mb-20">
        <Pricing />
      </div>
    </div>
  );
}

```

```default|ts|Pricing.tsx
import { Button } from "@registry/packages/primitives/buttons/02/default-ts/Button";
import type { IconProps } from "@registry/utils/types";

export function PricingGrid() {
  const PricingData = [
    {
      name: "Content",
      value: null,
    },
    {
      name: "Number of Users",
      value: ["100", "10,000", "Unlimited"],
    },
    {
      name: "Storage",
      value: ["10 GB", "100 GB", "500 GB"],
    },
    {
      name: "Requests per month",
      value: ["10,000", "10,00,000", "Unlimited"],
    },
    {
      name: "Team members",
      value: ["Just You", "Upto 10", "Unlimited"],
    },
    {
      name: "Early Access",
      value: [false, true, true],
    },
    {
      name: "Support",
      value: null,
    },
    {
      name: "Email Support",
      value: [false, true, true],
    },
    {
      name: "Phone Support",
      value: [false, true, true],
    },
    {
      name: "24/7 Support",
      value: [false, false, true],
    },
  ];

  return (
    <table className="w-full text-foreground">
      <colgroup>
        <col className="w-1/4" />
        <col className="w-1/4" />
        <col className="w-1/4" />
        <col className="w-1/4" />
      </colgroup>
      <thead>
        <td />
        <TableHead name="Basic" targetAudience="For individuals" Amount="0" ButtonText="Get Started" popular={false} />

        <TableHead name="Pro" targetAudience="For Startups" Amount="29" ButtonText="Purchase Now" popular={true} />

        <TableHead
          name="Enterprise"
          targetAudience="For Enterprises"
          Amount="49"
          ButtonText="Contact Sales"
          popular={false}
        />
      </thead>

      <tbody>
        {PricingData.map((data, index) => (
          <TableRow key={index} name={data.name} value={data.value} />
        ))}
      </tbody>
    </table>
  );
}

function TableHead({
  name,
  targetAudience,
  Amount,
  ButtonText,
  popular,
}: {
  name: string;
  targetAudience: string;
  Amount: string;
  ButtonText: string;
  popular: boolean;
}) {
  return (
    <td className="px-2 rounded-t-2xl py-6">
      <h1 className="text-2xl font-medium mb-2">{name}</h1>
      <h2 className="text-md mb-4 text-secondary-foreground">{targetAudience}</h2>
      <h1 className="text-4xl font-medium mb-8 mt-6 flex gap-2 items-end">
        ${Amount}
        <span className="text-secondary-foreground font-medium text-base">/ Month</span>
      </h1>
      <Button variant={popular ? "accent" : "primary"} className="w-full">
        {ButtonText}
      </Button>
    </td>
  );
}

function TableRow({
  name,
  value,
}: {
  name: string;
  value: Array<string | boolean> | null;
}) {
  return (
    <>
      {value ? (
        <tr className="h-16 text-secondary-foreground">
          <td>{name}</td>
          {value.map((value, index) => (
            // Values of the row
            <td className="text-center" key={index}>
              {typeof value === "string" ? (
                // Incase of string Values
                value
              ) : // In case of boolean values
              value ? (
                <Check className="text-accent mx-auto w-6 h-6" />
              ) : (
                <Cross className="text-secondary-foreground opacity-50 mx-auto w-5 h-5" />
              )}
            </td>
          ))}
        </tr>
      ) : (
        <>
          <tr className="h-20 border-b border-outline-secondary ">
            <td className="font-semibold">
              <div className="w-full min-h-12 " />
              {name}

              <div className="w-full min-h-6 " />
            </td>
            <td />
            <td />
            <td />
          </tr>
        </>
      )}
    </>
  );
}

export const Check = (props: IconProps) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

export const Cross = (props: IconProps) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 15 15" {...props}>
    <title>Cross</title>
    <path
      fill="currentColor"
      d="M3.64 2.27L7.5 6.13l3.84-3.84A.92.92 0 0 1 12 2a1 1 0 0 1 1 1a.9.9 0 0 1-.27.66L8.84 7.5l3.89 3.89A.9.9 0 0 1 13 12a1 1 0 0 1-1 1a.92.92 0 0 1-.69-.27L7.5 8.87l-3.85 3.85A.92.92 0 0 1 3 13a1 1 0 0 1-1-1a.9.9 0 0 1 .27-.66L6.16 7.5L2.27 3.61A.9.9 0 0 1 2 3a1 1 0 0 1 1-1c.24.003.47.1.64.27Z"
    />
  </svg>
);

```

</CodeBlock>
<Details>


</Details>
</Implementation>
</WrapperToggle>
</Wrapper>

{/*
// ------------------------------------------------------------------------------------
    //	The text below is generated from "@/registry/components/pricing/07"
//------------------------------------------------------------------------------------
*/}

<Wrapper>
<Title>Detailed Pricing Component with highlighted Most Popular</Title>
### The Detailed Pricing Component provides an in-depth look at product offerings, featuring multiple sections that clearly outline the features, benefits, and pricing tiers. Each section is designed to present essential information in an organized manner, helping users easily compare different plans.
<WrapperToggle>
<Preview>

export const Components_pricing_07 = lazy(() => import("@registry/packages/components/pricing/07/default-ts/example"));


<Suspense fallback={<Skeleton className="h-[800px] rounded-lg" />}>
<ResizableDisplay>
<Components_pricing_07 />
</ResizableDisplay>
</Suspense>

</Preview><Implementation>
undefined
<CodeBlock>

```default|js|example.jsx
import { Pricing } from "@registry/packages/components/pricing/05/default-js/Pricing";
import { PricingGrid } from "@registry/packages/components/pricing/07/default-js/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full flex justify-center items-center">
      <div className="hidden @lg:block w-[90%] mt-40 mb-20">
        <PricingGrid />
      </div>
      <div className="@lg:hidden w-[90%] mt-40 mb-20">
        <Pricing />
      </div>
    </div>
  );
}

```

```default|js|Pricing.jsx
import { Button } from "@registry/packages/primitives/buttons/02/default-js/Button";
import { twMerge } from "tailwind-merge";

export function PricingGrid() {
  const popular = 1;
  const backgroundOfPopular = "bg-neutral-900";

  const PricingData = [
    {
      name: "Content",
      value: null,
    },
    {
      name: "Number of Users",
      value: ["100", "10,000", "Unlimited"],
    },
    {
      name: "Storage",
      value: ["10 GB", "100 GB", "500 GB"],
    },
    {
      name: "Requests per month",
      value: ["10,000", "10,00,000", "Unlimited"],
    },
    {
      name: "Team members",
      value: ["Just You", "Upto 10", "Unlimited"],
    },
    {
      name: "Early Access",
      value: [false, true, true],
    },
    {
      name: "Support",
      value: null,
    },
    {
      name: "Email Support",
      value: [false, true, true],
    },
    {
      name: "Phone Support",
      value: [false, true, true],
    },
    {
      name: "24/7 Support",
      value: [false, false, true],
    },
  ];

  return (
    <table className="w-full text-foreground">
      <colgroup>
        <col className="w-1/4" />
        <col className={twMerge("w-1/4", popular === 0 && backgroundOfPopular)} />
        <col className={twMerge("w-1/4", popular === 1 && backgroundOfPopular)} />
        <col className={twMerge("w-1/4", popular === 2 && backgroundOfPopular)} />
      </colgroup>
      <thead>
        <tr>
          <td />
          <TableHead
            name="Basic"
            targetAudience="For individuals"
            Amount="0"
            ButtonText="Get Started"
            popular={popular === 0}
          />

          <TableHead
            name="Pro"
            targetAudience="For Startups"
            Amount="29"
            ButtonText="Purchase Now"
            popular={popular === 1}
          />

          <TableHead
            name="Enterprise"
            targetAudience="For Enterprises"
            Amount="49"
            ButtonText="Contact Sales"
            popular={popular === 2}
          />
        </tr>
      </thead>

      <tbody>
        {PricingData.map((data, index) => (
          <TableRow key={index} name={data.name} value={data.value} />
        ))}
        <tr className="h-10">
          <td />
          <td className={twMerge("rounded-b-xl", popular === 0 && backgroundOfPopular)} />
          <td className={twMerge("rounded-b-xl", popular === 1 && backgroundOfPopular)} />
          <td className={twMerge("rounded-b-xl", popular === 2 && backgroundOfPopular)} />
        </tr>
      </tbody>
    </table>
  );
}

function TableHead({ name, targetAudience, Amount, ButtonText, popular }) {
  return (
    <td className="px-4 rounded-t-2xl py-6">
      <h1 className="text-2xl font-medium mb-2">{name}</h1>
      <h2 className="text-md mb-4 text-secondary-foreground">{targetAudience}</h2>
      <h1 className="text-4xl font-medium mb-8 mt-6 flex gap-2 items-end">
        ${Amount}
        <span className="text-secondary-foreground font-medium text-base">/ Month</span>
      </h1>
      <Button variant={popular ? "accent" : "primary"} className="w-full">
        {ButtonText}
      </Button>
    </td>
  );
}

function TableRow({ name, value }) {
  return (
    <>
      {value ? (
        <tr className="h-16 text-secondary-foreground">
          <td>{name}</td>
          {value.map((value, index) => (
            // Values of the row
            <td className="text-center" key={index}>
              {typeof value === "string" ? (
                // Incase of string Values
                value
              ) : // In case of boolean values
              value ? (
                <Check className="text-accent mx-auto w-6 h-6" />
              ) : (
                <Cross className="text-secondary-foreground opacity-50 mx-auto w-5 h-5" />
              )}
            </td>
          ))}
        </tr>
      ) : (
        <>
          <tr className="h-20 border-b border-outline-secondary ">
            <td className="font-semibold">
              <div className="w-full min-h-12 " />
              {name}

              <div className="w-full min-h-6 " />
            </td>
            <td />
            <td />
            <td />
          </tr>
        </>
      )}
    </>
  );
}

export const Check = (props) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);

export const Cross = (props) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 15 15" {...props}>
    <title>Cross</title>
    <path
      fill="currentColor"
      d="M3.64 2.27L7.5 6.13l3.84-3.84A.92.92 0 0 1 12 2a1 1 0 0 1 1 1a.9.9 0 0 1-.27.66L8.84 7.5l3.89 3.89A.9.9 0 0 1 13 12a1 1 0 0 1-1 1a.92.92 0 0 1-.69-.27L7.5 8.87l-3.85 3.85A.92.92 0 0 1 3 13a1 1 0 0 1-1-1a.9.9 0 0 1 .27-.66L6.16 7.5L2.27 3.61A.9.9 0 0 1 2 3a1 1 0 0 1 1-1c.24.003.47.1.64.27Z"
    />
  </svg>
);

```

```default|ts|example.tsx
import { Pricing } from "@registry/packages/components/pricing/05/default-ts/Pricing";
import { PricingGrid } from "@registry/packages/components/pricing/07/default-ts/Pricing";

export default function PricingImplementation() {
  return (
    <div className="w-full flex justify-center items-center">
      <div className="hidden @lg:block w-[90%] mt-40 mb-20">
        <PricingGrid />
      </div>
      <div className="@lg:hidden w-[90%] mt-40 mb-20">
        <Pricing />
      </div>
    </div>
  );
}

```

```default|ts|Pricing.tsx
import { Button } from "@registry/packages/primitives/buttons/02/default-ts/Button";
import type { IconProps } from "@registry/utils/types";
import { twMerge } from "tailwind-merge";

export function PricingGrid() {
  const popular: number = 1;
  const backgroundOfPopular: string = "bg-neutral-900";

  const PricingData = [
    {
      name: "Content",
      value: null,
    },
    {
      name: "Number of Users",
      value: ["100", "10,000", "Unlimited"],
    },
    {
      name: "Storage",
      value: ["10 GB", "100 GB", "500 GB"],
    },
    {
      name: "Requests per month",
      value: ["10,000", "10,00,000", "Unlimited"],
    },
    {
      name: "Team members",
      value: ["Just You", "Upto 10", "Unlimited"],
    },
    {
      name: "Early Access",
      value: [false, true, true],
    },
    {
      name: "Support",
      value: null,
    },
    {
      name: "Email Support",
      value: [false, true, true],
    },
    {
      name: "Phone Support",
      value: [false, true, true],
    },
    {
      name: "24/7 Support",
      value: [false, false, true],
    },
  ];

  return (
    <table className="w-full text-foreground">
      <colgroup>
        <col className="w-1/4" />
        <col className={twMerge("w-1/4", popular === 0 && backgroundOfPopular)} />
        <col className={twMerge("w-1/4", popular === 1 && backgroundOfPopular)} />
        <col className={twMerge("w-1/4", popular === 2 && backgroundOfPopular)} />
      </colgroup>
      <thead>
        <tr>
          <td />
          <TableHead
            name="Basic"
            targetAudience="For individuals"
            Amount="0"
            ButtonText="Get Started"
            popular={popular === 0}
          />

          <TableHead
            name="Pro"
            targetAudience="For Startups"
            Amount="29"
            ButtonText="Purchase Now"
            popular={popular === 1}
          />

          <TableHead
            name="Enterprise"
            targetAudience="For Enterprises"
            Amount="49"
            ButtonText="Contact Sales"
            popular={popular === 2}
          />
        </tr>
      </thead>

      <tbody>
        {PricingData.map((data, index) => (
          <TableRow key={index} name={data.name} value={data.value} />
        ))}
        <tr className="h-10">
          <td />
          <td className={twMerge("rounded-b-xl", popular === 0 && backgroundOfPopular)} />
          <td className={twMerge("rounded-b-xl", popular === 1 && backgroundOfPopular)} />
          <td className={twMerge("rounded-b-xl", popular === 2 && backgroundOfPopular)} />
        </tr>
      </tbody>
    </table>
  );
}

function TableHead({
  name,
  targetAudience,
  Amount,
  ButtonText,
  popular,
}: {
  name: string;
  targetAudience: string;
  Amount: string;
  ButtonText: string;
  popular: boolean;
}) {
  return (
    <td className="px-4 rounded-t-2xl py-6">
      <h1 className="text-2xl font-medium mb-2">{name}</h1>
      <h2 className="text-md mb-4 text-secondary-foreground">{targetAudience}</h2>
      <h1 className="text-4xl font-medium mb-8 mt-6 flex gap-2 items-end">
        ${Amount}
        <span className="text-secondary-foreground font-medium text-base">/ Month</span>
      </h1>
      <Button variant={popular ? "accent" : "primary"} className="w-full">
        {ButtonText}
      </Button>
    </td>
  );
}

function TableRow({
  name,
  value,
}: {
  name: string;
  value: Array<string | boolean> | null;
}) {
  return (
    <>
      {value ? (
        <tr className="h-16 text-secondary-foreground">
          <td>{name}</td>
          {value.map((value, index) => (
            // Values of the row
            <td className="text-center" key={index}>
              {typeof value === "string" ? (
                // Incase of string Values
                value
              ) : // In case of boolean values
              value ? (
                <Check className="text-accent mx-auto w-6 h-6" />
              ) : (
                <Cross className="text-secondary-foreground opacity-50 mx-auto w-5 h-5" />
              )}
            </td>
          ))}
        </tr>
      ) : (
        <>
          <tr className="h-20 border-b border-outline-secondary ">
            <td className="font-semibold">
              <div className="w-full min-h-12 " />
              {name}

              <div className="w-full min-h-6 " />
            </td>
            <td />
            <td />
            <td />
          </tr>
        </>
      )}
    </>
  );
}

export const Check = (props: IconProps) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 16 16" {...props}>
    <title>Tick Mark</title>
    <path
      fill="currentColor"
      fillRule="evenodd"
      d="M13.488 3.43a.75.75 0 0 1 .081 1.058l-6 7a.75.75 0 0 1-1.1.042l-3.5-3.5A.75.75 0 0 1 4.03 6.97l2.928 2.927l5.473-6.385a.75.75 0 0 1 1.057-.081Z"
      clipRule="evenodd"
    />
  </svg>
);
export const Cross = (props: IconProps) => (
  <svg xmlns="http://www.w3.org/2000/svg" width="1em" height="1em" viewBox="0 0 15 15" {...props}>
    <title>Cross</title>
    <path
      fill="currentColor"
      d="M3.64 2.27L7.5 6.13l3.84-3.84A.92.92 0 0 1 12 2a1 1 0 0 1 1 1a.9.9 0 0 1-.27.66L8.84 7.5l3.89 3.89A.9.9 0 0 1 13 12a1 1 0 0 1-1 1a.92.92 0 0 1-.69-.27L7.5 8.87l-3.85 3.85A.92.92 0 0 1 3 13a1 1 0 0 1-1-1a.9.9 0 0 1 .27-.66L6.16 7.5L2.27 3.61A.9.9 0 0 1 2 3a1 1 0 0 1 1-1c.24.003.47.1.64.27Z"
    />
  </svg>
);

```

</CodeBlock>
<Details>


</Details>
</Implementation>
</WrapperToggle>
</Wrapper>
