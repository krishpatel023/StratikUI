import React, { Suspense } from 'react';
import { Implementation, Preview, Header, TechnologyUsed, Wrapper, Skeleton, PARTITION } from "@/components/mdx/MDXServerImports"
import { CodeBlock, ResizableDisplay } from "@/components/mdx/MDXClientImports"


{/*
// ------------------------------------------------------------------------------------
//	The text below is generated from apps/web/src/content/docs/components/cookie-consent/CookieConsent_01.mdx
// ------------------------------------------------------------------------------------
*/}


<Wrapper>

# Default Cookie Consent
### This component is paired up with useCookies hook. It uses various functionalities related to user preferences and is just a good demo for that. Click Cookie Settings to see it in action.





export const CookieImplementation_components_cookie_consent_cookieconsent_01 = React.lazy(() =>
	import('@/packages/components/cookie_consent/Cookie_1')
		.then((mod) => ({
			default: mod.CookieImplementation
	}))
);


<Header />
<Preview> 

<Suspense fallback={<Skeleton className="h-[800px] rounded-lg" />}>
<ResizableDisplay>
<CookieImplementation_components_cookie_consent_cookieconsent_01 />
</ResizableDisplay>
</Suspense>

</Preview>

<Implementation>


<TechnologyUsed technologies={[ "tailwind-css" , "twMerge", "react-aria"]}/>

<CodeBlock>
 
```tsx|CookieConsent
"use client";

import useCookies, {
  CookieCategory,
} from "@/packages/hooks/useCookies/useCookies";
import { Button } from "@/packages/primitives/buttons/Button_2";
import { Modal, ModalTrigger } from "@/packages/primitives/modal/Modal_1";
import { Switch } from "@/packages/primitives/switch/Switch_1";
import { useState } from "react";

export const CookiePrompt = ({
  active,
  setActive,
}: {
  active: boolean;
  setActive: React.Dispatch<React.SetStateAction<boolean>>;
}) => {
  const [details, setDetails] = useState(false);

  const message = {
    cookie:
      "Cookies are used to store information about your preferences and usage of the site. We use cookies to ensure that you get the best experience on our site.",
    details: {
      essential:
        "Essential cookies are necessary for the website to function properly and provide basic features.",
      functional:
        "Functional cookies enhance the website's functionality by remembering your preferences, such as language settings, display options, or recently viewed items.",
      analytics:
        "Analytics cookies collect data about your usage of the website, such as pages visited, time spent on the website, and interactions with site elements.",
      advertising:
        "These cookies often involve sharing your data with third-party advertisers and ad networks.",
    },
  };

  const {
    cookiePreferences,
    toggleCookieCategory,
    rejectAllCookiePreferences,
    acceptAllCookiePreferences,
  } = useCookies();

  return (
    <>
      <ModalTrigger
        isOpen={active && !details}
        onOpenChange={(open) => setActive(open)}
      >
        <Button className="hidden" aria-hidden="true">
          Close
        </Button>
        <Modal
          position="right bottom"
          className="max-w-[25rem] bg-background border border-outline-secondary"
        >
          <div className="w-full @md:max-w-80">
            <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
            <p className="mb-8 text-secondary-foreground font-medium">
              {message.cookie}
            </p>
            <div className="flex gap-4">
              <Button
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background"
                variant="complementary"
                onClick={() => {
                  acceptAllCookiePreferences();
                  setActive(false);
                }}
              >
                Accept All
              </Button>
              <button
                onClick={() => setDetails(true)}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Cookie Settings
              </button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>

      <ModalTrigger
        isOpen={active && details}
        onOpenChange={(open) => {
          setActive(open);
          setDetails(false);
        }}
      >
        <Button className="hidden" aria-hidden="true">
          Close
        </Button>
        <Modal
          position="right bottom"
          className="max-w-[25rem] bg-background border border-outline-secondary"
        >
          <div className="max-w-[23rem]">
            <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
            {Object.keys(cookiePreferences).map((key) => {
              return (
                <div
                  className="flex gap-2 justify-center items-center mb-4"
                  key={key}
                >
                  <span className="w-[90%]">
                    <h1 className="text-base font-semibold capitalize mb-1">
                      {key}
                    </h1>
                    <p className="text-sm text-secondary-foreground font-medium">
                      {message.details[key as CookieCategory]}
                    </p>
                  </span>
                  <Switch
                    isSelected={cookiePreferences[key as CookieCategory]}
                    onChange={() => toggleCookieCategory(key as CookieCategory)}
                  />
                </div>
              );
            })}
            <div className="flex gap-4">
              <button
                onClick={() => {
                  acceptAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background "
              >
                Accept All
              </button>
              <button
                onClick={() => {
                  rejectAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Reject All
              </button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>
    </>
  );
};
```
```jsx|CookieConsent
"use client";

import useCookies, {
  CookieCategory,
} from "@/packages/hooks/useCookies/useCookies";
import { Button } from "@/packages/primitives/buttons/Button_2";
import { Modal, ModalTrigger } from "@/packages/primitives/modal/Modal_1";
import { Switch } from "@/packages/primitives/switch/Switch_1";
import { useState } from "react";

export const CookiePrompt = ({
  active,
  setActive,
}) => {
  const [details, setDetails] = useState(false);

  const message = {
    cookie:
      "Cookies are used to store information about your preferences and usage of the site. We use cookies to ensure that you get the best experience on our site.",
    details: {
      essential:
        "Essential cookies are necessary for the website to function properly and provide basic features.",
      functional:
        "Functional cookies enhance the website's functionality by remembering your preferences, such as language settings, display options, or recently viewed items.",
      analytics:
        "Analytics cookies collect data about your usage of the website, such as pages visited, time spent on the website, and interactions with site elements.",
      advertising:
        "These cookies often involve sharing your data with third-party advertisers and ad networks.",
    },
  };

  const {
    cookiePreferences,
    toggleCookieCategory,
    rejectAllCookiePreferences,
    acceptAllCookiePreferences,
  } = useCookies();

  return (
    <>
      <ModalTrigger
        isOpen={active && !details}
        onOpenChange={(open) => setActive(open)}
      >
        <Button className="hidden" aria-hidden="true">
          Close
        </Button>
        <Modal
          position="right bottom"
          className="max-w-[25rem] bg-background border border-outline-secondary"
        >
          <div className="w-full @md:max-w-80">
            <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
            <p className="mb-8 text-secondary-foreground font-medium">
              {message.cookie}
            </p>
            <div className="flex gap-4">
              <Button
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background"
                variant="complementary"
                onClick={() => {
                  acceptAllCookiePreferences();
                  setActive(false);
                }}
              >
                Accept All
              </Button>
              <button
                onClick={() => setDetails(true)}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Cookie Settings
              </button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>

      <ModalTrigger
        isOpen={active && details}
        onOpenChange={(open) => {
          setActive(open);
          setDetails(false);
        }}
      >
        <Button className="hidden" aria-hidden="true">
          Close
        </Button>
        <Modal
          position="right bottom"
          className="max-w-[25rem] bg-background border border-outline-secondary"
        >
          <div className="max-w-[23rem]">
            <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
            {Object.keys(cookiePreferences).map((key) => {
              return (
                <div
                  className="flex gap-2 justify-center items-center mb-4"
                  key={key}
                >
                  <span className="w-[90%]">
                    <h1 className="text-base font-semibold capitalize mb-1">
                      {key}
                    </h1>
                    <p className="text-sm text-secondary-foreground font-medium">
                      {message.details[key as CookieCategory]}
                    </p>
                  </span>
                  <Switch
                    isSelected={cookiePreferences[key as CookieCategory]}
                    onChange={() => toggleCookieCategory(key as CookieCategory)}
                  />
                </div>
              );
            })}
            <div className="flex gap-4">
              <button
                onClick={() => {
                  acceptAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background "
              >
                Accept All
              </button>
              <button
                onClick={() => {
                  rejectAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Reject All
              </button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>
    </>
  );
};
```

```tsx|Implementation
export function CookieImplementation() {
  const [active, setActive] = useState(false);
  return (
    <div className="w-full min-h-60 flex justify-center items-center">
      <CookiePrompt active={active} setActive={setActive} />
      <Button onClick={() => setActive(true)}>Open Modal</Button>
    </div>
  );
}
```
```jsx|Implementation
export function CookieImplementation() {
  const [active, setActive] = useState(false);
  return (
    <div className="w-full min-h-60 flex justify-center items-center">
      <CookiePrompt active={active} setActive={setActive} />
      <Button onClick={() => setActive(true)}>Open Modal</Button>
    </div>
  );
}
```

</CodeBlock>



</Implementation>
</Wrapper>
<PARTITION />


{/*
// ------------------------------------------------------------------------------------
//	The text below is generated from apps/web/src/content/docs/components/cookie-consent/CookieConsent_02.mdx
// ------------------------------------------------------------------------------------
*/}


<Wrapper>

# Centered Cookie Consent
### This component is paired up with useCookies hook. It uses various functionalities related to user preferences and is just a good demo for that. Click Cookie Settings to see it in action.





export const CookieImplementation_components_cookie_consent_cookieconsent_02 = React.lazy(() =>
	import('@/packages/components/cookie_consent/Cookie_2')
		.then((mod) => ({
			default: mod.CookieImplementation
	}))
);


<Header />
<Preview> 

<Suspense fallback={<Skeleton className="h-[800px] rounded-lg" />}>
<ResizableDisplay>
<CookieImplementation_components_cookie_consent_cookieconsent_02 />
</ResizableDisplay>
</Suspense>

</Preview>

<Implementation>


<TechnologyUsed technologies={[ "tailwind-css" , "twMerge", "react-aria"]}/>

<CodeBlock>

```tsx|CookieConsent
"use client";

import useCookies, {
  CookieCategory,
} from "@/packages/hooks/useCookies/useCookies";
import { Button } from "@/packages/primitives/buttons/Button_2";
import { Modal, ModalTrigger } from "@/packages/primitives/modal/Modal_1";
import { Switch } from "@/packages/primitives/switch/Switch_1";
import { useState } from "react";

export const CookiePrompt = ({
  active,
  setActive,
}: {
  active: boolean;
  setActive: React.Dispatch<React.SetStateAction<boolean>>;
}) => {
  const [details, setDetails] = useState(false);

  const message = {
    cookie:
      "Cookies are used to store information about your preferences and usage of the site. We use cookies to ensure that you get the best experience on our site.",
    details: {
      essential:
        "Essential cookies are necessary for the website to function properly and provide basic features.",
      functional:
        "Functional cookies enhance the website's functionality by remembering your preferences, such as language settings, display options, or recently viewed items.",
      analytics:
        "Analytics cookies collect data about your usage of the website, such as pages visited, time spent on the website, and interactions with site elements.",
      advertising:
        "These cookies often involve sharing your data with third-party advertisers and ad networks.",
    },
  };

  const {
    cookiePreferences,
    toggleCookieCategory,
    rejectAllCookiePreferences,
    acceptAllCookiePreferences,
  } = useCookies();

  return (
    <>
      <ModalTrigger
        isOpen={active && !details}
        onOpenChange={(open) => setActive(open)}
      >
        <Modal
          position="center bottom"
          className="max-w-[35rem] md:max-w-[45rem] bg-background border border-outline-secondary"
        >
          <div className="w-full @md:max-w-80">
            <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
            <p className="mb-8 text-secondary-foreground font-medium">
              {message.cookie}
            </p>
            <div className="flex gap-4">
              <Button
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background"
                variant="complementary"
                onClick={() => {
                  acceptAllCookiePreferences();
                  setActive(false);
                }}
              >
                Accept All
              </Button>
              <button
                onClick={() => setDetails(true)}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Cookie Settings
              </button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>

      <ModalTrigger
        isOpen={active && details}
        onOpenChange={(open) => {
          setActive(open);
          setDetails(false);
        }}
      >
        <Button className="hidden" aria-hidden="true">
          Close
        </Button>
        <Modal
          position="right bottom"
          className="max-w-[25rem] bg-background border border-outline-secondary"
        >
          <div className="max-w-[23rem]">
            <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
            {Object.keys(cookiePreferences).map((key) => {
              return (
                <div
                  className="flex gap-2 justify-center items-center mb-4"
                  key={key}
                >
                  <span className="w-[90%]">
                    <h1 className="text-base font-semibold capitalize mb-1">
                      {key}
                    </h1>
                    <p className="text-sm text-secondary-foreground font-medium">
                      {message.details[key as CookieCategory]}
                    </p>
                  </span>
                  <Switch
                    isSelected={cookiePreferences[key as CookieCategory]}
                    onChange={() => toggleCookieCategory(key as CookieCategory)}
                  />
                </div>
              );
            })}
            <div className="flex gap-4">
              <button
                onClick={() => {
                  acceptAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background "
              >
                Accept All
              </button>
              <button
                onClick={() => {
                  rejectAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Reject All
              </button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>
    </>
  );
};
```
```jsx|CookieConsent
"use client";

import useCookies, {
  CookieCategory,
} from "@/packages/hooks/useCookies/useCookies";
import { Button } from "@/packages/primitives/buttons/Button_2";
import { Modal, ModalTrigger } from "@/packages/primitives/modal/Modal_1";
import { Switch } from "@/packages/primitives/switch/Switch_1";
import { useState } from "react";

export const CookiePrompt = ({
  active,
  setActive,
}) => {
  const [details, setDetails] = useState(false);

  const message = {
    cookie:
      "Cookies are used to store information about your preferences and usage of the site. We use cookies to ensure that you get the best experience on our site.",
    details: {
      essential:
        "Essential cookies are necessary for the website to function properly and provide basic features.",
      functional:
        "Functional cookies enhance the website's functionality by remembering your preferences, such as language settings, display options, or recently viewed items.",
      analytics:
        "Analytics cookies collect data about your usage of the website, such as pages visited, time spent on the website, and interactions with site elements.",
      advertising:
        "These cookies often involve sharing your data with third-party advertisers and ad networks.",
    },
  };

  const {
    cookiePreferences,
    toggleCookieCategory,
    rejectAllCookiePreferences,
    acceptAllCookiePreferences,
  } = useCookies();

  return (
    <>
      <ModalTrigger
        isOpen={active && !details}
        onOpenChange={(open) => setActive(open)}
      >
        <Button className="hidden" aria-hidden="true">
          Close
        </Button>
        <Modal
          position="center bottom"
          className="max-w-[35rem] md:max-w-[45rem] bg-background border border-outline-secondary"
        >
          <div className="w-full @md:max-w-80">
            <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
            <p className="mb-8 text-secondary-foreground font-medium">
              {message.cookie}
            </p>
            <div className="flex gap-4">
              <Button
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background"
                variant="complementary"
                onClick={() => {
                  acceptAllCookiePreferences();
                  setActive(false);
                }}
              >
                Accept All
              </Button>
              <button
                onClick={() => setDetails(true)}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Cookie Settings
              </button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>

      <ModalTrigger
        isOpen={active && details}
        onOpenChange={(open) => {
          setActive(open);
          setDetails(false);
        }}
      >
        <Modal
          position="right bottom"
          className="max-w-[25rem] bg-background border border-outline-secondary"
        >
          <div className="max-w-[23rem]">
            <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
            {Object.keys(cookiePreferences).map((key) => {
              return (
                <div
                  className="flex gap-2 justify-center items-center mb-4"
                  key={key}
                >
                  <span className="w-[90%]">
                    <h1 className="text-base font-semibold capitalize mb-1">
                      {key}
                    </h1>
                    <p className="text-sm text-secondary-foreground font-medium">
                      {message.details[key as CookieCategory]}
                    </p>
                  </span>
                  <Switch
                    isSelected={cookiePreferences[key as CookieCategory]}
                    onChange={() => toggleCookieCategory(key as CookieCategory)}
                  />
                </div>
              );
            })}
            <div className="flex gap-4">
              <button
                onClick={() => {
                  acceptAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background "
              >
                Accept All
              </button>
              <button
                onClick={() => {
                  rejectAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Reject All
              </button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>
    </>
  );
};
```
```tsx|Implementation
export function CookieImplementation() {
  const [active, setActive] = useState(false);
  return (
    <div className="w-full min-h-60 flex justify-center items-center">
      <CookiePrompt active={active} setActive={setActive} />
      <Button onClick={() => setActive(true)}>Open Modal</Button>
    </div>
  );
}
```
```jsx|Implementation
export function CookieImplementation() {
  const [active, setActive] = useState(false);
  return (
    <div className="w-full min-h-60 flex justify-center items-center">
      <CookiePrompt active={active} setActive={setActive} />
      <Button onClick={() => setActive(true)}>Open Modal</Button>
    </div>
  );
}
```

</CodeBlock>



</Implementation>
</Wrapper>
<PARTITION />


{/*
// ------------------------------------------------------------------------------------
//	The text below is generated from apps/web/src/content/docs/components/cookie-consent/CookieConsent_03.mdx
// ------------------------------------------------------------------------------------
*/}


<Wrapper>

# Centered flexed Cookie Consent
### This component is paired up with useCookies hook. It uses various functionalities related to user preferences and is just a good demo for that. Click Cookie Settings to see it in action.





export const CookieImplementation_components_cookie_consent_cookieconsent_03 = React.lazy(() =>
	import('@/packages/components/cookie_consent/Cookie_3')
		.then((mod) => ({
			default: mod.CookieImplementation
	}))
);


<Header />
<Preview> 

<Suspense fallback={<Skeleton className="h-[800px] rounded-lg" />}>
<ResizableDisplay>
<CookieImplementation_components_cookie_consent_cookieconsent_03 />
</ResizableDisplay>
</Suspense>

</Preview>

<Implementation>


<TechnologyUsed technologies={[ "tailwind-css" , "twMerge", "react-aria"]}/>

<CodeBlock>

```tsx|CookieConsent
"use client";

import useCookies, {
  CookieCategory,
} from "@/packages/hooks/useCookies/useCookies";
import { Button } from "@/packages/primitives/buttons/Button_2";
import { Modal, ModalTrigger } from "@/packages/primitives/modal/Modal_1";
import { Switch } from "@/packages/primitives/switch/Switch_1";
import { useState } from "react";

export const CookiePrompt = ({
  active,
  setActive,
}: {
  active: boolean;
  setActive: React.Dispatch<React.SetStateAction<boolean>>;
}) => {
  const [details, setDetails] = useState(false);

  const message = {
    cookie:
      "Cookies are used to store information about your preferences and usage of the site. We use cookies to ensure that you get the best experience on our site.",
    details: {
      essential:
        "Essential cookies are necessary for the website to function properly and provide basic features.",
      functional:
        "Functional cookies enhance the website's functionality by remembering your preferences, such as language settings, display options, or recently viewed items.",
      analytics:
        "Analytics cookies collect data about your usage of the website, such as pages visited, time spent on the website, and interactions with site elements.",
      advertising:
        "These cookies often involve sharing your data with third-party advertisers and ad networks.",
    },
  };

  const {
    cookiePreferences,
    toggleCookieCategory,
    rejectAllCookiePreferences,
    acceptAllCookiePreferences,
  } = useCookies();

  return (
    <>
      <ModalTrigger
        isOpen={active && !details}
        onOpenChange={(open) => setActive(open)}
      >
        <Modal
          position="center bottom"
          className="max-w-[35rem] md:max-w-[70rem] bg-background border border-outline-secondary"
        >
          <div className="w-full flex flex-col md:flex-row">
            <div className="w-full md:w-3/4">
              {" "}
              <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
              <p className="mb-8 text-secondary-foreground font-medium">
                {message.cookie}
              </p>
            </div>
            <div className="flex gap-4 justify-start md:justify-center items-center">
              <Button
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background"
                variant="complementary"
                onClick={() => {
                  acceptAllCookiePreferences();
                  setActive(false);
                }}
              >
                Accept
              </Button>
              <Button
                variant="outline"
                onClick={() => setDetails(true)}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Cookie Settings
              </Button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>

      <ModalTrigger
        isOpen={active && details}
        onOpenChange={(open) => {
          setActive(open);
          setDetails(false);
        }}
      >
        <Modal
          position="right bottom"
          className="max-w-[25rem] bg-background border border-outline-secondary"
        >
          <div className="max-w-[23rem]">
            <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
            {Object.keys(cookiePreferences).map((key) => {
              return (
                <div
                  className="flex gap-2 justify-center items-center mb-4"
                  key={key}
                >
                  <span className="w-[90%]">
                    <h1 className="text-base font-semibold capitalize mb-1">
                      {key}
                    </h1>
                    <p className="text-sm text-secondary-foreground font-medium">
                      {message.details[key as CookieCategory]}
                    </p>
                  </span>
                  <Switch
                    isSelected={cookiePreferences[key as CookieCategory]}
                    onChange={() => toggleCookieCategory(key as CookieCategory)}
                  />
                </div>
              );
            })}
            <div className="flex gap-4">
              <Button
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background"
                variant="complementary"
                onClick={() => {
                  acceptAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
              >
                Accept
              </Button>
              <Button
                variant="outline"
                onClick={() => {
                  rejectAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Cookie Settings
              </Button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>
    </>
  );
};
```
```jsx|CookieConsent
"use client";

import useCookies, {
  CookieCategory,
} from "@/packages/hooks/useCookies/useCookies";
import { Button } from "@/packages/primitives/buttons/Button_2";
import { Modal, ModalTrigger } from "@/packages/primitives/modal/Modal_1";
import { Switch } from "@/packages/primitives/switch/Switch_1";
import { useState } from "react";

export const CookiePrompt = ({
  active,
  setActive,
}: {
  active: boolean;
  setActive: React.Dispatch<React.SetStateAction<boolean>>;
}) => {
  const [details, setDetails] = useState(false);

  const message = {
    cookie:
      "Cookies are used to store information about your preferences and usage of the site. We use cookies to ensure that you get the best experience on our site.",
    details: {
      essential:
        "Essential cookies are necessary for the website to function properly and provide basic features.",
      functional:
        "Functional cookies enhance the website's functionality by remembering your preferences, such as language settings, display options, or recently viewed items.",
      analytics:
        "Analytics cookies collect data about your usage of the website, such as pages visited, time spent on the website, and interactions with site elements.",
      advertising:
        "These cookies often involve sharing your data with third-party advertisers and ad networks.",
    },
  };

  const {
    cookiePreferences,
    toggleCookieCategory,
    rejectAllCookiePreferences,
    acceptAllCookiePreferences,
  } = useCookies();

  return (
    <>
      <ModalTrigger
        isOpen={active && !details}
        onOpenChange={(open) => setActive(open)}
      >
        <Modal
          position="center bottom"
          className="max-w-[35rem] md:max-w-[70rem] bg-background border border-outline-secondary"
        >
          <div className="w-full flex flex-col md:flex-row">
            <div className="w-full md:w-3/4">
              {" "}
              <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
              <p className="mb-8 text-secondary-foreground font-medium">
                {message.cookie}
              </p>
            </div>
            <div className="flex gap-4 justify-start md:justify-center items-center">
              <Button
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background"
                variant="complementary"
                onClick={() => {
                  acceptAllCookiePreferences();
                  setActive(false);
                }}
              >
                Accept
              </Button>
              <Button
                variant="outline"
                onClick={() => setDetails(true)}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Cookie Settings
              </Button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>

      <ModalTrigger
        isOpen={active && details}
        onOpenChange={(open) => {
          setActive(open);
          setDetails(false);
        }}
      >
        <Modal
          position="right bottom"
          className="max-w-[25rem] bg-background border border-outline-secondary"
        >
          <div className="max-w-[23rem]">
            <h1 className="text-xl font-medium mb-4">Privacy Policy</h1>
            {Object.keys(cookiePreferences).map((key) => {
              return (
                <div
                  className="flex gap-2 justify-center items-center mb-4"
                  key={key}
                >
                  <span className="w-[90%]">
                    <h1 className="text-base font-semibold capitalize mb-1">
                      {key}
                    </h1>
                    <p className="text-sm text-secondary-foreground font-medium">
                      {message.details[key as CookieCategory]}
                    </p>
                  </span>
                  <Switch
                    isSelected={cookiePreferences[key as CookieCategory]}
                    onChange={() => toggleCookieCategory(key as CookieCategory)}
                  />
                </div>
              );
            })}
            <div className="flex gap-4">
              <Button
                className="py-1 px-3 rounded-md font-medium bg-foreground text-background"
                variant="complementary"
                onClick={() => {
                  acceptAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
              >
                Accept
              </Button>
              <Button
                variant="outline"
                onClick={() => {
                  rejectAllCookiePreferences();
                  setDetails(false);
                  setActive(false);
                }}
                className="py-1 px-3 rounded-md font-medium border-2 border-foreground text-foreground "
              >
                Cookie Settings
              </Button>
            </div>
          </div>
        </Modal>
      </ModalTrigger>
    </>
  );
};
```

```tsx|Implementation
export function CookieImplementation() {
  const [active, setActive] = useState(false);
  return (
    <div className="w-full min-h-60 flex justify-center items-center">
      <CookiePrompt active={active} setActive={setActive} />
      <Button onClick={() => setActive(true)}>Open Modal</Button>
    </div>
  );
}
```
```jsx|Implementation
export function CookieImplementation() {
  const [active, setActive] = useState(false);
  return (
    <div className="w-full min-h-60 flex justify-center items-center">
      <CookiePrompt active={active} setActive={setActive} />
      <Button onClick={() => setActive(true)}>Open Modal</Button>
    </div>
  );
}
```


</CodeBlock>



</Implementation>
</Wrapper>
<PARTITION />
