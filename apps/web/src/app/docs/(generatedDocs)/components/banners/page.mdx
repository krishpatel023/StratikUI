import React, { Suspense } from 'react';
import { Implementation, Preview, Header, TechnologyUsed, Wrapper, Skeleton, PARTITION } from "@/components/mdx/MDXServerImports"
import { CodeBlock, ResizableDisplay } from "@/components/mdx/MDXClientImports"


{/*
// ------------------------------------------------------------------------------------
//	The text below is generated from apps/web/src/content/docs/components/banners/Banners_01.mdx
// ------------------------------------------------------------------------------------
*/}


<Wrapper>

# Simple Banner
### This is a simple banner that can be used to display important information.





export const BannerImplementation_components_banners_banners_01 = React.lazy(() =>
	import('@/packages/components/banners/Banner_1')
		.then((mod) => ({
			default: mod.BannerImplementation
	}))
);


<Header />
<Preview> 

<Suspense fallback={<Skeleton className="h-[800px] rounded-lg" />}>
<ResizableDisplay>
<BannerImplementation_components_banners_banners_01 />
</ResizableDisplay>
</Suspense>

</Preview>

<Implementation>


<TechnologyUsed technologies={[ "tailwind-css" , "twMerge", "react-aria"]}/>

<CodeBlock>
 
```tsx|Banner
"use client";

import { Button } from "@/packages/primitives/buttons/Button_2";
import { GradientBackground } from "@/packages/primitives/containers/Container_1";
import { useState } from "react";
import { twMerge } from "tailwind-merge";
export function Banner({
  open,
  setOpen,
  children,
  className,
}: {
  open: boolean;
  setOpen?: React.Dispatch<React.SetStateAction<boolean>>;
  children: React.ReactNode;
  className?: string;
}) {
  return (
    <div
      className={twMerge(
        "w-full absolute left-0 transition-all duration-300 ease-in-out",
        open ? "top-0" : "-top-full",
        className
      )}
    >
      {children}
    </div>
  );
}

```
```jsx|Banner
"use client";

import { Button } from "@/packages/primitives/buttons/Button_2";
import { GradientBackground } from "@/packages/primitives/containers/Container_1";
import { useState } from "react";
import { twMerge } from "tailwind-merge";
export function Banner({
  open,
  setOpen,
  children,
  className,
}) {
  return (
    <div
      className={twMerge(
        "w-full absolute left-0 transition-all duration-300 ease-in-out",
        open ? "top-0" : "-top-full",
        className
      )}
    >
      {children}
    </div>
  );
}
```

```tsx|Implementation
export function BannerImplementation() {
  const [open, setOpen] = useState(false);
  return (
    <div className="w-full relative">
      <Banner
        className="flex justify-center items-center py-2 px-6 bg-foreground text-background gap-8"
        open={open}
        setOpen={setOpen}
      >
        <h1 className="font-semibold">
          Checkout the latest release of the components that are available for
          free.
        </h1>
        <GradientBackground className="absolute z-0">
          <button className="py-2 px-4 rounded-md bg-background text-foreground z-10 relative">
            Get Started
          </button>
        </GradientBackground>
      </Banner>
      <div className="min-w-full min-h-[40rem] flex justify-center items-center">
        <Button onClick={() => setOpen(!open)}>Toggle Banner</Button>
      </div>
    </div>
  );
}
```
```jsx|Implementation
export function BannerImplementation() {
  const [open, setOpen] = useState(false);
  return (
    <div className="w-full relative">
      <Banner
        className="flex justify-center items-center py-2 px-6 bg-foreground text-background gap-8"
        open={open}
        setOpen={setOpen}
      >
        <h1 className="font-semibold">
          Checkout the latest release of the components that are available for
          free.
        </h1>
        <GradientBackground className="absolute z-0">
          <button className="py-2 px-4 rounded-md bg-background text-foreground z-10 relative">
            Get Started
          </button>
        </GradientBackground>
      </Banner>
      <div className="min-w-full min-h-[40rem] flex justify-center items-center">
        <Button onClick={() => setOpen(!open)}>Toggle Banner</Button>
      </div>
    </div>
  );
}
```

</CodeBlock>


</Implementation>
</Wrapper>
<PARTITION />
