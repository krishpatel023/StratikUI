import React, { Suspense } from 'react';
import { Implementation, Preview, Header, Display, TechnologyUsed, Wrapper, Skeleton, PARTITION } from "@/components/mdx/MDXServerImports"
import { CodeBlock } from "@/components/mdx/MDXClientImports"


{/*
// ------------------------------------------------------------------------------------
//	The text below is generated from apps/web/src/content/docs/primitives/modal/Modal_01.mdx
// ------------------------------------------------------------------------------------
*/}


<Wrapper>

# Modal
### A modal is highly customizable and includes features like styleable states for entry and exit animations, customizable underlay and overlay elements, and accessibility enhancements that hide content outside the modal from assistive technologies. It also manages focus by moving it into the modal on mount and restoring it to the trigger element on unmount, preventing users from tabbing outside. Additionally, a modal prevents page scrolling while open, including on mobile browsers.





export const ModalImplementation_primitives_modal_modal_01 = React.lazy(() =>
	import('@/packages/primitives/modal/Modal_1')
		.then((mod) => ({
			default: mod.ModalImplementation
	}))
);


<Header />
<Preview> 

<Suspense fallback={<Skeleton className="h-60 rounded-lg" />}>
<Display>
    <ModalImplementation_primitives_modal_modal_01 />
</Display>
</Suspense>

</Preview>

<Implementation>


<TechnologyUsed technologies={[ "tailwind-css" , "twMerge", "react-aria" ]}/>

<CodeBlock>
 
```tsx|Modal
"use client";

import {
  Button,
  Dialog,
  DialogProps,
  DialogTrigger,
  DialogTriggerProps,
  Modal as ModalAria,
  ModalOverlay,
} from "react-aria-components";
import { twMerge } from "tailwind-merge";

export interface ModalProps extends DialogProps {
  position?:
    | "center center"
    | "center top"
    | "center bottom"
    | "left top"
    | "left center"
    | "left bottom"
    | "right top"
    | "right bottom"
    | "right center";
  isBGBlur?: boolean;
  isDismissable?: boolean;
}

export function Modal({
  children,
  isDismissable = true,
  position = "center center",
  isBGBlur = true,
  className,
  ...props
}: ModalProps) {
  return (
    <ModalOverlay
      isDismissable={isDismissable}
      className={twMerge(
        "w-[100dvw] h-[100dvh] top-0 fixed z-[9999] data-[entering=true]:animate-modal-fade-in data-[exiting=true]:animate-modal-fade-out",
        isBGBlur && "bg-neutral-900/60"
      )}
    >
      <ModalAria className="outline-none ">
        <Dialog
          className={twMerge(
            "absolute w-full h-max py-6 px-6 rounded bg-primary border border-outline-secondary text-primary-foreground outline-none max-w-[30rem] data-[entering=true]:animate-modal-zoom",
            position === "center center" &&
              "top-1/2 left-1/2 -translate-y-1/2 -translate-x-1/2",
            position === "center top" && "top-6 left-1/2 -translate-x-1/2",
            position === "center bottom" &&
              "bottom-6 left-1/2 -translate-x-1/2",
            position === "left top" && "top-6 left-6",
            position === "left center" && "top-1/2 left-6 -translate-y-1/2",
            position === "left bottom" && "bottom-6 left-6",
            position === "right top" && "top-6 right-6",
            position === "right bottom" && "bottom-6 right-6",
            position === "right center" && "top-1/2 right-6 -translate-y-1/2",
            className
          )}
          {...props}
        >
          {children}
        </Dialog>
      </ModalAria>
    </ModalOverlay>
  );
}

export function ModalTrigger({ ...props }: DialogTriggerProps) {
  return <DialogTrigger {...props}>{props.children}</DialogTrigger>;
}
```
```jsx|Modal
"use client";

import {
  Button,
  Dialog,
  DialogTrigger,
  Modal as ModalAria,
  ModalOverlay,
} from "react-aria-components";
import { twMerge } from "tailwind-merge";

export function Modal({
  children,
  isDismissable = true,
  position = "center center",
  isBGBlur = true,
  className,
  ...props
}) {
  return (
    <ModalOverlay
      isDismissable={isDismissable}
      className={twMerge(
        "w-[100dvw] h-[100dvh] top-0 fixed z-[9999] data-[entering=true]:animate-modal-fade-in data-[exiting=true]:animate-modal-fade-out",
        isBGBlur && "bg-neutral-900/60"
      )}
    >
      <ModalAria className="outline-none ">
        <Dialog
          className={twMerge(
            "absolute w-full h-max py-6 px-6 rounded bg-primary border border-outline-secondary text-primary-foreground outline-none max-w-[30rem] data-[entering=true]:animate-modal-zoom",
            position === "center center" &&
              "top-1/2 left-1/2 -translate-y-1/2 -translate-x-1/2",
            position === "center top" && "top-6 left-1/2 -translate-x-1/2",
            position === "center bottom" &&
              "bottom-6 left-1/2 -translate-x-1/2",
            position === "left top" && "top-6 left-6",
            position === "left center" && "top-1/2 left-6 -translate-y-1/2",
            position === "left bottom" && "bottom-6 left-6",
            position === "right top" && "top-6 right-6",
            position === "right bottom" && "bottom-6 right-6",
            position === "right center" && "top-1/2 right-6 -translate-y-1/2",
            className
          )}
          {...props}
        >
          {children}
        </Dialog>
      </ModalAria>
    </ModalOverlay>
  );
}

export function ModalTrigger({ ...props }) {
  return <DialogTrigger {...props}>{props.children}</DialogTrigger>;
}
```

```ts|tailwind.config
import type { Config } from "tailwindcss";
const config: Config = {
  content: [ ... ],
  theme: {
    extend: {
      animation: {
        "modal-fade-in": "modal-fade 0.3s ease-in",
        "modal-fade-out": "modal-fade 0.3s reverse ease-in",
        "modal-zoom": "modal-zoom 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275)",
      },
      keyframes: {
        "modal-fade": {
          "0%": {
            opacity: "0",
          },
          "100%": {
            opacity: "1",
          },
        },
        "modal-zoom": {
          "0%": {
            transform: "scale(0.8)",
          },
          "100%": {
            transform: "scale(1)",
          },
        },
      },
    },
  },
  plugins: [ ... ],
};
export default config;
```
```js|tailwind.config
const config = {
  content: [ ... ],
  theme: {
    extend: {
      animation: {
        "modal-fade-in": "modal-fade 0.3s ease-in",
        "modal-fade-out": "modal-fade 0.3s reverse ease-in",
        "modal-zoom": "modal-zoom 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275)",
      },
      keyframes: {
        "modal-fade": {
          "0%": {
            opacity: "0",
          },
          "100%": {
            opacity: "1",
          },
        },
        "modal-zoom": {
          "0%": {
            transform: "scale(0.8)",
          },
          "100%": {
            transform: "scale(1)",
          },
        },
      },
    },
  },
  plugins: [ ... ],
};
export default config;
```

```tsx|Implementation
export function ModalImplementation() {
  return (
    <ModalTrigger>
      <Button className="bg-primary hover:bg-secondary text-primary-foreground px-2 py-2 rounded border border-outline-secondary mt-4">
        Open Modal
      </Button>
      <Modal>
        {({ close }) => (
          <>
            <h1 className="text-xl font-semibold text-primary-foreground mb-4">
              This is a modal
            </h1>
            <span>
              It has various various positions and can be dismissed by:
            </span>
            <ul className="list-disc ml-4 mt-4">
              <li>Clicking outside the modal</li>
              <li>Pressing the escape key</li>
              <li>Clicking the close button</li>
            </ul>
            <Button
              onPress={close}
              className="bg-primary hover:bg-secondary text-primary-foreground px-2 py-1 rounded border border-outline-secondary mt-4"
            >
              Close
            </Button>
          </>
        )}
      </Modal>
    </ModalTrigger>
  );
}
```
```jsx|Implementation
export function ModalImplementation() {
  return (
    <ModalTrigger>
      <Button className="bg-primary hover:bg-secondary text-primary-foreground px-2 py-2 rounded border border-outline-secondary mt-4">
        Open Modal
      </Button>
      <Modal>
        {({ close }) => (
          <>
            <h1 className="text-xl font-semibold text-primary-foreground mb-4">
              This is a modal
            </h1>
            <span>
              It has various various positions and can be dismissed by:
            </span>
            <ul className="list-disc ml-4 mt-4">
              <li>Clicking outside the modal</li>
              <li>Pressing the escape key</li>
              <li>Clicking the close button</li>
            </ul>
            <Button
              onPress={close}
              className="bg-primary hover:bg-secondary text-primary-foreground px-2 py-1 rounded border border-outline-secondary mt-4"
            >
              Close
            </Button>
          </>
        )}
      </Modal>
    </ModalTrigger>
  );
}
```

</CodeBlock>



Checkout the default documentation of [React Aria Modal](https://react-spectrum.adobe.com/react-aria/Modal.html)

</Implementation>
</Wrapper>
<PARTITION />
