import React, { Suspense, lazy } from 'react';
import { Display, TechnologyUsed, Wrapper, Skeleton, Title, Details, Provider } from "@/components/mdx/MDXServerImports"
import { CodeBlock } from "@/components/mdx/MDXClientImports"

{/*
// ------------------------------------------------------------------------------------
    //	The text below is generated from "@/registry/hooks/useOS/01"
//------------------------------------------------------------------------------------
*/}

<Wrapper>
<Title>useOS</Title>
### The `useOS` hook provides a simple way to detect the user's operating system based on the browser's user agent string. This can be useful for adjusting functionality or UI elements based on the detected OS.

<CodeBlock variant="useos">

```default|js|example.jsx
"use client";
import { useOS } from "@registry/packages/hooks/useOS/01/default-js/useOS";

export default function UseOSExample() {
  const os = useOS();

  return (
    <div className="w-full h-60 flex flex-col justify-center items-center text-foreground">
      <p>Your operating system is: {os}</p>
    </div>
  );
}

```

```default|js|useOS.jsx
"use client";

import { useState, useEffect } from "react";

export const useOS = () => {
  const [os, setOS] = useState("Unknown");

  useEffect(() => {
    const userAgent = window.navigator.userAgent;

    const detectOS = () => {
      if (/Windows/i.test(userAgent)) {
        return "Windows";
      }
      if (/Macintosh|MacIntel|MacPPC|Mac68K/i.test(userAgent)) {
        return "MacOS";
      }
      if (/Linux/i.test(userAgent)) {
        return "Linux";
      }
      if (/Android/i.test(userAgent)) {
        return "Android";
      }
      if (/iPhone|iPad|iPod/i.test(userAgent)) {
        return "iOS";
      }
      return "Unknown";
    };

    setOS(detectOS());
  }, []);

  return os;
};

```

```default|ts|example.tsx
"use client";
import { useOS } from "@registry/packages/hooks/useOS/01/default-ts/useOS";

export default function UseOSExample() {
  const os = useOS();

  return (
    <div className="w-full h-60 flex flex-col justify-center items-center text-foreground">
      <p>Your operating system is: {os}</p>
    </div>
  );
}

```

```default|ts|useOS.tsx
"use client";

import { useState, useEffect } from "react";

export type OS = "Windows" | "MacOS" | "Linux" | "Android" | "iOS" | "Unknown";

export const useOS = (): OS => {
  const [os, setOS] = useState<OS>("Unknown");

  useEffect(() => {
    const userAgent = window.navigator.userAgent;

    const detectOS = (): OS => {
      if (/Windows/i.test(userAgent)) {
        return "Windows";
      }
      if (/Macintosh|MacIntel|MacPPC|Mac68K/i.test(userAgent)) {
        return "MacOS";
      }
      if (/Linux/i.test(userAgent)) {
        return "Linux";
      }
      if (/Android/i.test(userAgent)) {
        return "Android";
      }
      if (/iPhone|iPad|iPod/i.test(userAgent)) {
        return "iOS";
      }
      return "Unknown";
    };

    setOS(detectOS());
  }, []);

  return os;
};

```

</CodeBlock>
<Details>


### Return Table

| Name | Type | Description                                                                                                                   |
| ---- | ---- | ----------------------------------------------------------------------------------------------------------------------------- |
| os   | `OS` | A string representing the detected operating system: `"Windows"`, `"MacOS"`, `"Linux"`, `"Android"`, `"iOS"`, or `"Unknown"`. |

</Details>
export const Hooks_useos_01 = lazy(() => import("@registry/packages/hooks/useOS/01/default-ts/example"));

<Suspense fallback={<Skeleton className="h-60 rounded-lg" />}>
<Display>
<Hooks_useos_01 />
</Display>
</Suspense>

</Wrapper>
