import React, { Suspense } from 'react';
import { Display, TechnologyUsed, Wrapper, Skeleton } from "@/components/mdx/MDXServerImports"
import { CodeBlock } from "@/components/mdx/MDXClientImports"


{/*
// ------------------------------------------------------------------------------------
//	The text below is generated from apps/web/src/content/docs/hooks/useClipboard/useClipboard.mdx
// ------------------------------------------------------------------------------------
*/}


<Wrapper>

# useClipboard
### The useClipboard hook makes it easy to use clipboard with its two functions: copy and read.





export const Helper_hooks_useclipboard_useclipboard = React.lazy(() =>
	import('@/packages/hooks/useClipboard/Demo.tsx')
		.then((mod) => ({
			default: mod.Helper
	}))
);


<CodeBlock>
 
```tsx|useClipboard
"use client";

export default function useClipboard() {
  const copy = async (text: string, callback?: () => void) => {
    try {
      await navigator.clipboard.writeText(text);
      if (callback) callback();
    } catch (err) {
      console.error("Failed to write to clipboard", err);
    }
  };

  const read = async () => {
    try {
      const text = await navigator.clipboard.readText();
      return text;
    } catch (err) {
      console.error("Failed to read clipboard contents", err);
    }
  };

  return {
    copy,
    read,
  };
}
```
```jsx|useClipboard
"use client";

export default function useClipboard() {
  const copy = async (text, callback) => {
    try {
      await navigator.clipboard.writeText(text);
      if (callback) callback();
    } catch (err) {
      console.error("Failed to write to clipboard", err);
    }
  };

  const read = async () => {
    try {
      const text = await navigator.clipboard.readText();
      return text;
    } catch (err) {
      console.error("Failed to read clipboard contents", err);
    }
  };

  return {
    copy,
    read,
  };
}
```

```tsx|Implementation
"use client";

import { useState } from "react";
import useClipboard from "./useClipboard";

export default function Helper() {
  const { copy, read } = useClipboard();

  const [value, setValue] = useState("Hello, World!");

  const handleCopy = () => {
    copy("Hello, World!");
  };

  const handleRead = async () => {
    const text = await read();
    if (text) setValue(text);
  };

  return (
    <div className="dark:text-white flex flex-col justify-center items-center gap-8 px-8">
      <div className="flex gap-4">
        <Button onClick={handleCopy}>Copy</Button>
        <Button onClick={handleRead}>Read</Button>
      </div>

      <h1 className="text-2xl underline">Coppied Value</h1>
      <p>{value}</p>
    </div>
  );
}
```
```jsx|Implementation
"use client";

import { useState } from "react";
import useClipboard from "./useClipboard";

export default function Helper() {
  const { copy, read } = useClipboard();

  const [value, setValue] = useState("Hello, World!");

  const handleCopy = () => {
    copy("Hello, World!");
  };

  const handleRead = async () => {
    const text = await read();
    if (text) setValue(text);
  };

  return (
    <div className="dark:text-white flex flex-col justify-center items-center gap-8 px-8">
      <div className="flex gap-4">
        <Button onClick={handleCopy}>Copy</Button>
        <Button onClick={handleRead}>Read</Button>
      </div>

      <h1 className="text-2xl underline">Coppied Value</h1>
      <p>{value}</p>
    </div>
  );
}
```

</CodeBlock>



<Suspense fallback={<Skeleton className="h-60 rounded-lg" />}>
<Display>
    <Helper_hooks_useclipboard_useclipboard/>
</Display>
</Suspense>
</Wrapper>